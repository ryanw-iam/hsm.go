---
phase: 02-deterministic-runtime-semantics
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - hsm.go
  - hsm_test_helpers_test.go
  - hsm_runtime_timers_test.go
  - hsm_runtime_concurrency_test.go
  - hsm_runtime_lifecycle_test.go
  - hsm_test.go
autonomous: true
requirements:
  - VER-03
must_haves:
  truths:
    - "Stop, restart, and termination-timeout behavior can be verified through deterministic synchronization rather than incidental delays."
    - "Race-enabled verification passes on the deterministic runtime scenarios added in this phase."
    - "If a runtime bug blocks deterministic lifecycle assertions, the fix stays scoped to the root runtime semantics needed for Phase 2."
  artifacts:
    - path: hsm_runtime_lifecycle_test.go
      provides: Focused deterministic coverage for Stop, Restart, termination timeout, cancellation, and related lifecycle observations
    - path: hsm_test_helpers_test.go
      provides: Lifecycle-oriented helpers for bounded waits and deterministic error/timeout observation
    - path: hsm_runtime_timers_test.go
      provides: Timer-suite tightening if the final race-enabled gate exposes remaining deterministic issues there
    - path: hsm_runtime_concurrency_test.go
      provides: Concurrency-suite tightening if the final race-enabled gate exposes remaining deterministic issues there
    - path: hsm.go
      provides: Minimal runtime synchronization or lifecycle fixes only if the new deterministic tests expose a real Phase 2 defect
  key_links:
    - from: hsm_runtime_lifecycle_test.go
      to: hsm.go
      via: Stop, Restart, terminate timeout, and AfterExecuted observation
      pattern: "Stop\\(|Restart\\(|terminate\\(|AfterExecuted"
    - from: hsm_runtime_lifecycle_test.go
      to: scripts/verify-workspace.sh
      via: final phase gate using the canonical verification entrypoint
      pattern: "go test -race -short"
---

<objective>
Lock lifecycle-sensitive runtime semantics behind deterministic tests and close Phase 2 with race-backed verification.

Purpose: Finish `VER-03` by covering cancellation and restart behavior, surfacing any minimal runtime fixes required for determinism, and proving the canonical verification gate stays green on the strengthened root-runtime suite.
Output: Focused lifecycle tests plus any narrowly-scoped runtime fix needed to make them deterministic and race-clean.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deterministic-runtime-semantics/02-RESEARCH.md
@.planning/phases/02-deterministic-runtime-semantics/02-VALIDATION.md
@.planning/phases/02-deterministic-runtime-semantics/02-01-PLAN.md
@.planning/phases/02-deterministic-runtime-semantics/02-02-PLAN.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-03-SUMMARY.md
@hsm.go
@scripts/verify-workspace.sh

<interfaces>
From hsm.go:

```go
func Stop(ctx context.Context, hsm Instance) <-chan struct{}
func Restart(ctx context.Context, hsm Instance, maybeData ...any) <-chan struct{}
func AfterExecuted[T stringLike](ctx context.Context, hsm Instance, state T) <-chan struct{}

func (sm *hsm[T]) terminate(ctx context.Context, element Element) {
	select {
	case <-maybeActive.channel:
	case <-sm.clock.After(sm.timeouts.activity):
		go sm.dispatch(ctx, ErrorEvent.WithData(fmt.Errorf("terminate timeout: %s", element.QualifiedName())))
	}
}
```

From scripts/verify-workspace.sh:

```bash
go test ./...
go test -race -short ./... ./kind/... ./muid/...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic lifecycle and termination-timeout coverage</name>
  <files>hsm_runtime_lifecycle_test.go, hsm_test_helpers_test.go, hsm.go</files>
  <action>Create focused lifecycle tests for `Stop`, `Restart`, activity cancellation, timeout-driven `ErrorEvent` dispatch during termination, and `AfterExecuted`/post-stop observation boundaries. Use injected clock control and waiter helpers so the timeout/error path is triggered deterministically. If these tests reveal a real runtime defect that prevents deterministic lifecycle assertions, patch `hsm.go` minimally and only inside the affected lifecycle/synchronization path; do not broaden into later-phase cleanup or helper-library changes. Risk handling: preserve the public runtime contract and keep any production change limited to making existing lifecycle behavior observable and repeatable.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <done>Lifecycle-sensitive runtime behaviors are covered through deterministic synchronization, and any blocking runtime fix is confined to the specific lifecycle path under test.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the deterministic runtime suite under the canonical race-enabled gate</name>
  <files>hsm_runtime_lifecycle_test.go, hsm_runtime_timers_test.go, hsm_runtime_concurrency_test.go, hsm_test_helpers_test.go, hsm_test.go, hsm.go</files>
  <action>Run the strengthened root-runtime suite under the same canonical verification entrypoint introduced in Phase 1 and remove any remaining race- or flake-causing assumptions within Phase 2 scope. Prefer tightening the timer, concurrency, lifecycle, and shared-helper suites first; only keep runtime changes that are required to satisfy real deterministic semantics. Use `hsm_test.go` only to delete or tighten residual overlapping legacy assertions that still interfere with the focused deterministic suites. Do not add a second verification script or expand coverage into `kind`, `muid`, or `rules.md` work beyond what the canonical gate already executes.</action>
  <verify>
    <automated>bash scripts/verify-workspace.sh</automated>
  </verify>
  <done>The canonical workspace gate passes, including its race-short run, with deterministic root-runtime semantics now covered by focused Phase 2 suites.</done>
</task>

</tasks>

<verification>
Run `go test ./...` after Task 1. Task 2 must finish on `bash scripts/verify-workspace.sh`, which remains the only blocking verification entrypoint for shipping.
</verification>

<success_criteria>
Root runtime lifecycle, timeout, and race-sensitive behaviors are covered deterministically, any required runtime fix is narrowly scoped to Phase 2 semantics, and the canonical workspace gate passes without introducing a second release path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-runtime-semantics/02-03-SUMMARY.md`
</output>
