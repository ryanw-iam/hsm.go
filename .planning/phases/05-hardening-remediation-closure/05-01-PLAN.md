---
phase: 05-hardening-remediation-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hsm_runtime_adversarial_test.go
  - hsm.go
autonomous: true
requirements:
  - VER-06
must_haves:
  truths:
    - "Public hostile-helper handling is consistent: `Stop(nil)` no longer remains a one-off panic seam while sibling helpers already degrade safely."
    - "The highest-priority remaining product seam is locked behind focused regression coverage that fails on helper-contract drift instead of surfacing as ad hoc debugging."
    - "Runtime remediation stays confined to the nil-stop helper seam and does not pull implicit completion transitions or broader lifecycle redesign into scope."
  artifacts:
    - path: "hsm_runtime_adversarial_test.go"
      provides: "Focused hostile-helper regression coverage that makes the nil-stop contract explicit"
    - path: "hsm.go"
      provides: "Narrow nil-instance stop handling aligned with the hardened helper surface"
  key_links:
    - from: "hsm_runtime_adversarial_test.go"
      to: "hsm.go"
      via: "The hostile-helper suite drives `Stop` through the same immediate-completion contract used by other nil-safe helpers"
      pattern: "Stop\\(|Dispatch\\(|Set\\("
    - from: "hsm.go"
      to: "hsm_runtime_lifecycle_test.go"
      via: "Nil-stop remediation must preserve the already-hardened stop lifecycle and timeout semantics"
      pattern: "stop\\(|TestRuntimeStop"
---

<objective>
Close the highest-priority remaining runtime seam by making `Stop(nil)` consistent with the hardened public helper contracts.

Purpose: Phase 4 surfaced that hostile helper handling had mostly converged on safe, closed-waiter behavior, but `Stop(nil)` still remained a one-off panic. Phase 5 starts by closing that exact seam and nothing broader.
Output: A focused hostile-helper regression plus the smallest `hsm.go` change needed to make `Stop(nil)` follow the established helper contract.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening-remediation-closure/05-RESEARCH.md
@.planning/phases/05-hardening-remediation-closure/05-VALIDATION.md
@.planning/phases/02-deterministic-runtime-semantics/02-03-SUMMARY.md
@.planning/phases/04-adversarial-regression-matrix/04-01-SUMMARY.md
@hsm.go
@hsm_runtime_adversarial_test.go
@hsm_runtime_lifecycle_test.go

<interfaces>
From hsm.go:

```go
func Dispatch[T context.Context](ctx T, hsm Instance, event Event) <-chan struct{}
func Set[T stringLike](ctx context.Context, hsm Instance, name T, value any) <-chan struct{}
func Stop(ctx context.Context, hsm Instance) <-chan struct{}
func AfterExecuted[T stringLike](ctx context.Context, hsm Instance, state T) <-chan struct{}
```

From hsm_runtime_adversarial_test.go:

```go
func TestPublicHelperAdversarial(t *testing.T)
```

From hsm_runtime_lifecycle_test.go:

```go
func TestRuntimeStopCancelsActivityAndClosesAfterExecuted(t *testing.T)
func TestRuntimeStopTimeoutDispatchesErrorEventDeterministically(t *testing.T)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock nil-stop helper consistency behind a focused hostile-helper regression</name>
  <files>hsm_runtime_adversarial_test.go, hsm.go</files>
  <behavior>
    - `Stop(nil)` closes immediately without panicking, matching the hostile-input contract already established for sibling helpers.
    - Existing typed-nil and missing-context helper behavior for `Dispatch`, `Set`, `Call`, `DispatchAll`, and `DispatchTo` remains intact while the new stop seam becomes explicit.
    - The change does not widen runtime scope into implicit completion transitions, alternate error APIs, or broader stop-path redesign.
  </behavior>
  <action>Update `TestPublicHelperAdversarial` in `hsm_runtime_adversarial_test.go` so the nil-stop case stops being mere panic characterization and instead asserts the same closed-waiter contract already used for other hostile helper inputs. Then make the smallest change in `hsm.go` needed for `Stop` to short-circuit nil instances safely. Keep the remediation confined to this public helper seam; do not reopen the already-fixed stop timeout, waiter, or queue handoff defects from Phase 2, and do not add any implicit completion semantics.</action>
  <verify>
    <automated>GOCACHE="$PWD/.cache/go-build" go test ./... -run 'TestPublicHelperAdversarial|TestRuntimeStopCancelsActivityAndClosesAfterExecuted|TestRuntimeStopTimeoutDispatchesErrorEventDeterministically'</automated>
  </verify>
  <done>`Stop(nil)` now matches the hardened hostile-helper contract, and the adjacent stop lifecycle regressions still pass unchanged.</done>
</task>

</tasks>

<verification>
Run `GOCACHE="$PWD/.cache/go-build" go test ./... -run 'TestPublicHelperAdversarial|TestRuntimeStopCancelsActivityAndClosesAfterExecuted|TestRuntimeStopTimeoutDispatchesErrorEventDeterministically'`. The nil-stop regression must pass without reopening the previously fixed Phase 2 stop-path seams.
</verification>

<success_criteria>
The last exposed helper-contract inconsistency is gone: `Stop(nil)` behaves like the rest of the hardened hostile helper surface, and the regression points directly at that contract if it ever drifts again.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-remediation-closure/05-01-SUMMARY.md`
</output>
