---
phase: 05-hardening-remediation-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - hsm.go
  - README.md
  - rules.md
autonomous: true
requirements:
  - VER-06
must_haves:
  truths:
    - "`rules.md`, exported comments, and `README.md` describe the same hardened synchronization contract."
    - "Waiter helpers are documented as deterministic observation or test helpers, while the returned completion channels remain the supported production synchronization path."
    - "`rules.md` is tracked as the contract artifact in the same plan that edits and aligns it."
    - "Public contract wording does not imply unsupported implicit completion transitions or other out-of-scope semantics."
  artifacts:
    - path: "rules.md"
      provides: "Tracked authoritative public rule wording for HSM53 and HSM54 plus adjacent helper-usage guidance"
    - path: "hsm.go"
      provides: "Exported helper comments aligned to the hardened public synchronization contract"
    - path: "README.md"
      provides: "User-facing package documentation that matches the hardened helper and waiter semantics"
  key_links:
    - from: "rules.md"
      to: "hsm.go"
      via: "HSM53 and HSM54 must align with the exported helper comments"
      pattern: "HSM53|HSM54|AfterProcess|AfterDispatch|AfterEntry|AfterExit|AfterExecuted|Stop\\("
    - from: "README.md"
      to: "hsm.go"
      via: "The README API descriptions must match the exported comment contract users see through package docs"
      pattern: "AfterProcess|AfterDispatch|AfterEntry|AfterExit|AfterExecuted|Stop"
---

<objective>
Align the public contract so the hardened runtime behavior, the rule set, and user-facing docs all say the same thing.

Purpose: Earlier phases tightened behavior quickly, but the package contract still drifts between `rules.md`, exported comments, and `README.md`, especially around waiter helpers and supported synchronization patterns. This plan removes that drift explicitly.
Output: Updated and tracked rule wording, exported comments, and README helper descriptions that all reflect the same hardened contract.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening-remediation-closure/05-RESEARCH.md
@.planning/phases/05-hardening-remediation-closure/05-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-04-SUMMARY.md
@.planning/phases/04-adversarial-regression-matrix/04-01-SUMMARY.md
@.planning/phases/05-hardening-remediation-closure/05-01-PLAN.md
@hsm.go
@README.md
@rules.md

<interfaces>
From rules.md:

```text
HSM53: ALWAYS wait on the channel returned by Dispatch, Set, Restart, Stop, DispatchAll, or DispatchTo before asserting on post-transition state.
HSM54: NEVER use hsm.AfterProcess(...), hsm.AfterDispatch(...), hsm.AfterEntry(...), hsm.AfterExit(...), or hsm.AfterExecuted(...) as production synchronization mechanisms.
```

From hsm.go:

```go
func AfterProcess(ctx context.Context, hsm Instance, maybeEvent ...Event) <-chan struct{}
func AfterDispatch(ctx context.Context, hsm Instance, event Event) <-chan struct{}
func AfterEntry[T stringLike](ctx context.Context, hsm Instance, state T) <-chan struct{}
func AfterExit[T stringLike](ctx context.Context, hsm Instance, state T) <-chan struct{}
func AfterExecuted[T stringLike](ctx context.Context, hsm Instance, state T) <-chan struct{}
func Stop(ctx context.Context, hsm Instance) <-chan struct{}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile rule text, exported comments, and README helper guidance</name>
  <files>hsm.go, README.md, rules.md</files>
  <action>Update `rules.md`, the exported helper comments in `hsm.go`, and the user-facing helper descriptions in `README.md` together so the package contract is consistent after the hardened helper work from Phases 3-5. Make HSM53 and HSM54 the anchor: completion channels returned by `Dispatch`, `Set`, `Restart`, `Stop`, `DispatchAll`, and `DispatchTo` are the supported production synchronization path, while `AfterProcess`, `AfterDispatch`, `AfterEntry`, `AfterExit`, and `AfterExecuted` are for tests and deterministic observation only. Remove stale wording that still implies general external synchronization utility, and ensure `rules.md` is tracked in git in this same plan so the aligned contract is not left as unversioned drift. Reflect the nil-stop consistency only insofar as it is part of the public helper contract. Do not broaden scope into new API shapes, implicit completion transitions, or another round of rules expansion.</action>
  <verify>
    <automated>bash -lc "rg -F 'HSM53: ALWAYS wait on the channel returned by \`Dispatch\`, \`Set\`, \`Restart\`, \`Stop\`, \`DispatchAll\`, or \`DispatchTo\` before asserting on post-transition state.' rules.md >/dev/null && rg -F 'HSM54: NEVER use \`hsm.AfterProcess(...)\`, \`hsm.AfterDispatch(...)\`, \`hsm.AfterEntry(...)\`, \`hsm.AfterExit(...)\`, or \`hsm.AfterExecuted(...)\` as production synchronization mechanisms.' rules.md >/dev/null && for f in hsm.go README.md; do rg -n 'completion channel|supported production synchronization path' \"$f\" >/dev/null && rg -n 'tests and deterministic observation only|deterministic observation' \"$f\" >/dev/null; done && ! rg -n 'coordinating external operations with state transitions|general synchronization' hsm.go README.md >/dev/null && git ls-files --error-unmatch rules.md >/dev/null && GOCACHE=\"$PWD/.cache/go-build\" go test ./... -run 'TestRuntimeRulesConformance|TestPublicHelperAdversarial'"</automated>
  </verify>
  <done>The rule set, package comments, and README all describe the same supported synchronization contract, stale general-synchronization wording is gone, and `rules.md` is tracked with the aligned contract change.</done>
</task>

</tasks>

<verification>
Run `bash -lc "rg -F 'HSM53: ALWAYS wait on the channel returned by \`Dispatch\`, \`Set\`, \`Restart\`, \`Stop\`, \`DispatchAll\`, or \`DispatchTo\` before asserting on post-transition state.' rules.md >/dev/null && rg -F 'HSM54: NEVER use \`hsm.AfterProcess(...)\`, \`hsm.AfterDispatch(...)\`, \`hsm.AfterEntry(...)\`, \`hsm.AfterExit(...)\`, or \`hsm.AfterExecuted(...)\` as production synchronization mechanisms.' rules.md >/dev/null && for f in hsm.go README.md; do rg -n 'completion channel|supported production synchronization path' \"$f\" >/dev/null && rg -n 'tests and deterministic observation only|deterministic observation' \"$f\" >/dev/null; done && ! rg -n 'coordinating external operations with state transitions|general synchronization' hsm.go README.md >/dev/null && git ls-files --error-unmatch rules.md >/dev/null && GOCACHE=\"$PWD/.cache/go-build\" go test ./... -run 'TestRuntimeRulesConformance|TestPublicHelperAdversarial'"`. The three public contract artifacts must agree on the supported synchronization model, stale wording must be absent, and the aligned `rules.md` must already be tracked here rather than deferred to a later hygiene step.
</verification>

<success_criteria>
Users get one consistent contract regardless of where they read it: `rules.md`, exported docs, and `README.md` all direct production callers to completion channels and treat waiter helpers as deterministic observation tools rather than general synchronization primitives, with the aligned `rules.md` tracked in the same remediation plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-remediation-closure/05-02-SUMMARY.md`
</output>
