---
phase: 05-hardening-remediation-closure
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - scripts/verify-workspace.sh
  - .travis.yml
autonomous: true
requirements:
  - VER-06
must_haves:
  truths:
    - "The canonical workspace gate reads as phase-neutral release evidence rather than leftover Phase 4 messaging."
    - "No stale legacy CI configuration remains in-tree, and the single canonical verifier passes end to end."
  artifacts:
    - path: "scripts/verify-workspace.sh"
      provides: "Phase-neutral canonical release gate wording over the already-established verification steps"
  key_links:
    - from: ".github/workflows/unit-tests.yml"
      to: "scripts/verify-workspace.sh"
      via: "Active CI must keep calling the single canonical verifier after the release-clean wording update"
      pattern: "bash scripts/verify-workspace.sh"
    - from: "scripts/verify-workspace.sh"
      to: "hsm_runtime_adversarial_test.go"
      via: "The final release gate still proves the hardened helper and runtime regressions through the existing entrypoint"
      pattern: "TestRuntimeAdversarial|TestPublicHelperAdversarial"
---

<objective>
Finish the release-clean closure by removing stale hygiene drift and proving the final canonical gate.

Purpose: The repository is already functionally green, but the final release surface still carries Phase 4 wording and obsolete Travis metadata. This plan closes those remaining hygiene gaps without inventing new verification scope.
Output: A phase-neutral canonical verifier, removal of stale legacy CI, and final end-to-end release evidence.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening-remediation-closure/05-RESEARCH.md
@.planning/phases/05-hardening-remediation-closure/05-VALIDATION.md
@.planning/phases/04-adversarial-regression-matrix/04-03-SUMMARY.md
@.planning/phases/05-hardening-remediation-closure/05-02-PLAN.md
@.github/workflows/unit-tests.yml
@scripts/verify-workspace.sh
@.travis.yml

<interfaces>
From scripts/verify-workspace.sh:

```bash
section "phase 4 workspace verification"
run_go_test "blocking: runtime adversarial suites" . -run 'TestRuntimeAdversarial|TestPublicHelperAdversarial'
run_go_test "blocking: race-short workspace" -race -short ./... ./kind/... ./muid/...
section "result"
echo "phase 4 workspace verification passed"
```

From .github/workflows/unit-tests.yml:

```yaml
- name: Run canonical workspace gate
  run: bash scripts/verify-workspace.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the release gate phase-neutral and close the remaining repository hygiene gaps</name>
  <files>scripts/verify-workspace.sh, .travis.yml</files>
  <action>Update `scripts/verify-workspace.sh` so its section and result wording describes the canonical workspace or release gate in phase-neutral terms while keeping the actual verification steps unchanged, and remove the stale `.travis.yml` legacy CI file now that GitHub Actions is the active path. Treat `rules.md` tracking as already owned and completed by Plan 05-02 rather than repeating or guessing at untracked-work cleanup here. Do not add new verification families, new scripts, or phase-specific wrappers; the single canonical entrypoint remains `bash scripts/verify-workspace.sh`.</action>
  <verify>
    <automated>bash -lc '! rg -n "phase 4 workspace verification|phase 4" scripts/verify-workspace.sh >/dev/null && rg -n "canonical workspace verification|workspace release gate|workspace verification passed|release gate passed" scripts/verify-workspace.sh >/dev/null && GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh && test ! -f .travis.yml'</automated>
  </verify>
  <done>The canonical verifier no longer contains the old Phase 4 wording, `.travis.yml` is gone, and the full release gate passes through the existing single entrypoint.</done>
</task>

</tasks>

<verification>
Run `bash -lc '! rg -n "phase 4 workspace verification|phase 4" scripts/verify-workspace.sh >/dev/null && rg -n "canonical workspace verification|workspace release gate|workspace verification passed|release gate passed" scripts/verify-workspace.sh >/dev/null && GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh && test ! -f .travis.yml'`. The workspace gate must stay single-entrypoint, prove the old phase-specific wording is gone from `scripts/verify-workspace.sh`, and remain free of stale legacy CI drift.
</verification>

<success_criteria>
Phase 5 closes with no known release-clean verification drift left open: the canonical gate no longer advertises obsolete Phase 4 wording, obsolete Travis metadata is gone, and the same gate passes end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-remediation-closure/05-03-SUMMARY.md`
</output>
