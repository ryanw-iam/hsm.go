---
phase: 06-deterministic-runtime-verification-backfill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hsm.go
  - hsm_runtime_timers_test.go
  - hsm_runtime_concurrency_test.go
  - hsm_runtime_lifecycle_test.go
  - hsm_test_helpers_test.go
autonomous: true
requirements:
  - VER-03
gap_closure: true
must_haves:
  truths:
    - "The shipped Phase 2 deterministic runtime claims are re-proven against current code instead of being trusted from old summaries alone."
    - "Any failure uncovered during re-verification is remediated only inside the existing Phase 2 deterministic runtime slice."
    - "The targeted deterministic runtime command and the canonical workspace gate both pass before any backfill docs claim closure."
  artifacts:
    - path: "hsm_runtime_timers_test.go"
      provides: "Focused deterministic timer, waiter, and clock-precedence coverage that Phase 2 already shipped"
    - path: "hsm_runtime_concurrency_test.go"
      provides: "Focused deterministic concurrency, queue replay, and multi-instance dispatch coverage"
    - path: "hsm_runtime_lifecycle_test.go"
      provides: "Focused deterministic lifecycle, stop, restart, and timeout coverage"
    - path: "hsm.go"
      provides: "Only the already-shipped runtime seam, reserved for tiny corrective fixes if re-verification proves the evidence false"
  key_links:
    - from: "hsm_runtime_timers_test.go"
      to: "hsm.go"
      via: "Deterministic timer and clock behavior exercised through the current runtime clock hooks"
      pattern: "After\\(|At\\(|Every\\(|When\\(|Config\\.Clock|DefaultClock"
    - from: "hsm_runtime_concurrency_test.go"
      to: "hsm.go"
      via: "Concurrent dispatch and multi-instance delivery assertions must still match the current runtime implementation"
      pattern: "Dispatch\\(|DispatchAll\\(|DispatchTo\\("
    - from: "hsm_runtime_lifecycle_test.go"
      to: "scripts/verify-workspace.sh"
      via: "The final deterministic runtime proof must survive the same canonical race-enabled gate used for shipping"
      pattern: "go test -race -short"
---

<objective>
Re-prove the shipped deterministic runtime slice before any Phase 2 backfill document claims closure.

Purpose: Phase 6 closes an audit orphan, not a feature gap. This plan verifies the current runtime truth first so the later evidence artifacts stay honest.
Output: Green deterministic runtime verification on current code, plus only a tiny Phase 2 runtime fix if re-verification proves an existing claim false.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-deterministic-runtime-verification-backfill/06-RESEARCH.md
@.planning/phases/06-deterministic-runtime-verification-backfill/06-VALIDATION.md
@.planning/phases/02-deterministic-runtime-semantics/02-VALIDATION.md
@.planning/phases/02-deterministic-runtime-semantics/02-01-SUMMARY.md
@.planning/phases/02-deterministic-runtime-semantics/02-02-SUMMARY.md
@.planning/phases/02-deterministic-runtime-semantics/02-03-SUMMARY.md
@hsm.go
@hsm_runtime_timers_test.go
@hsm_runtime_concurrency_test.go
@hsm_runtime_lifecycle_test.go
@hsm_test_helpers_test.go
@scripts/verify-workspace.sh

<interfaces>
From .planning/phases/06-deterministic-runtime-verification-backfill/06-VALIDATION.md:

```text
GOCACHE="$PWD/.cache/go-build" go test ./... -run 'TestRuntime(AfterUsesDeterministicTimer|AfterNegativeDurationDoesNotSchedule|AtUsesDeterministicTimer|EveryUsesManualTriggers|WhenWaitsForExplicitSignal|ConfigClockOverridesDefaultClock|DefaultClockFallbackUsesDeterministicTimer|ConcurrentDispatchSerializesProcessing|DeferredEventsReplayAfterTransition|CompletionEventsPreemptQueuedEvents|DispatchAllReachesEveryStartedInstance|DispatchToTargetsMatchingInstancesOnly|StopCancelsActivityAndClosesAfterExecuted|RestartReentersInitialStateWithData|StopTimeoutDispatchesErrorEventDeterministically)$' -count=1
GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh
```

From .planning/phases/02-deterministic-runtime-semantics/02-03-SUMMARY.md:

```text
Keep runtime changes limited to lifecycle observation and stop synchronization paths surfaced by the deterministic tests rather than broad queue or helper refactors.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the shipped deterministic runtime slice and keep any correction microscopic</name>
  <files>hsm.go, hsm_runtime_timers_test.go, hsm_runtime_concurrency_test.go, hsm_runtime_lifecycle_test.go, hsm_test_helpers_test.go</files>
  <action>Run the exact targeted deterministic runtime command from `06-VALIDATION.md`, then run `GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh`. If both commands pass, leave the runtime slice unchanged and capture the exact command/output facts for Plan 06-02. If re-verification fails because the current code disproves a shipped Phase 2 claim, fix only the smallest blocking issue inside the existing deterministic runtime slice listed in this plan. Keep the remediation inside current timer, concurrency, lifecycle, or shared test-helper seams; do not touch `kind`, `muid`, `rules.md`, `README.md`, workflows, or introduce any new deterministic-runtime feature work. If a truthful fix would require broader scope than these files, stop and surface that mismatch instead of widening the plan.</action>
  <verify>
    <automated>bash -lc 'GOCACHE="$PWD/.cache/go-build" go test ./... -run '\''TestRuntime(AfterUsesDeterministicTimer|AfterNegativeDurationDoesNotSchedule|AtUsesDeterministicTimer|EveryUsesManualTriggers|WhenWaitsForExplicitSignal|ConfigClockOverridesDefaultClock|DefaultClockFallbackUsesDeterministicTimer|ConcurrentDispatchSerializesProcessing|DeferredEventsReplayAfterTransition|CompletionEventsPreemptQueuedEvents|DispatchAllReachesEveryStartedInstance|DispatchToTargetsMatchingInstancesOnly|StopCancelsActivityAndClosesAfterExecuted|RestartReentersInitialStateWithData|StopTimeoutDispatchesErrorEventDeterministically)$'\'' -count=1 && GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh'</automated>
  </verify>
  <done>The current repository truth for Phase 2 is known from fresh command evidence, and any required correction stays microscopic inside the existing deterministic runtime slice.</done>
</task>

</tasks>

<verification>
Run the targeted deterministic runtime command from `06-VALIDATION.md`, then `GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh`. No later Phase 6 document should claim `VER-03` closure unless both commands are green on the same workspace state.
</verification>

<success_criteria>
Phase 6 has fresh executable proof for the current deterministic runtime slice, and any issue uncovered during that proof is either fixed only inside the shipped Phase 2 runtime seam or explicitly surfaced as out of scope for this backfill.
</success_criteria>

<output>
After completion, create `.planning/phases/06-deterministic-runtime-verification-backfill/06-01-SUMMARY.md`
</output>
