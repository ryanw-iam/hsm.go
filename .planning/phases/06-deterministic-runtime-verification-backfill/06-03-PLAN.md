---
phase: 06-deterministic-runtime-verification-backfill
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - VER-03
gap_closure: true
must_haves:
  truths:
    - "`REQUIREMENTS.md` no longer leaves `VER-03` pending once the backfilled Phase 2 verification artifact exists."
    - "The milestone audit stops reporting `VER-03` as orphaned because it can now see the real Phase 2 verification artifact."
    - "Traceability closure for `VER-03` does not hide the still-open Phase 7 and Phase 8 audit gaps."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Top-level requirement traceability showing `VER-03` complete through the Phase 6 backfill"
    - path: ".planning/v1.0-MILESTONE-AUDIT.md"
      provides: "Regenerated audit report that reflects the closed Phase 2 orphan without hand-edited fiction"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-deterministic-runtime-semantics/02-VERIFICATION.md"
      via: "Requirement closure must be backed by the real Phase 2 verification artifact, not summary frontmatter alone"
      pattern: "VER-03"
    - from: ".planning/v1.0-MILESTONE-AUDIT.md"
      to: ".planning/phases/02-deterministic-runtime-semantics/02-VERIFICATION.md"
      via: "The audit should stop flagging the missing Phase 2 verification report once the artifact exists"
      pattern: "02-VERIFICATION\\.md"
---

<objective>
Close the top-level `VER-03` traceability gap without hand-waving away the remaining milestone audit debt.

Purpose: The backfilled Phase 2 evidence only matters if the requirement table and milestone audit both recognize it. This plan repairs that traceability explicitly and honestly.
Output: `REQUIREMENTS.md` updated for `VER-03` and a regenerated milestone audit that no longer treats Phase 2 as orphaned.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/02-deterministic-runtime-semantics/02-VERIFICATION.md
@.planning/phases/02-deterministic-runtime-semantics/02-VALIDATION.md
@.planning/phases/06-deterministic-runtime-verification-backfill/06-02-SUMMARY.md
@/Users/gabrielwillen/.codex/get-shit-done/workflows/audit-milestone.md

<interfaces>
From .planning/REQUIREMENTS.md:

```text
- [ ] **VER-03**: Add deterministic validation for concurrency, timing, dispatch ordering, and race-sensitive behavior.
| VER-03 | Phase 6 | Pending |
```

From .planning/v1.0-MILESTONE-AUDIT.md:

```text
verification_status: "missing"
evidence: "Phase 2 summary frontmatter marks VER-03 complete, but .planning/phases/02-deterministic-runtime-semantics/02-VERIFICATION.md does not exist."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair `VER-03` traceability and regenerate the milestone audit from real evidence</name>
  <files>.planning/REQUIREMENTS.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <action>After confirming `02-VERIFICATION.md` exists and passes, update `REQUIREMENTS.md` so `VER-03` is marked complete and its traceability row reflects that Phase 6 closed the orphan by backfilling Phase 2 verification evidence. Then rerun the milestone audit workflow from `/Users/gabrielwillen/.codex/get-shit-done/workflows/audit-milestone.md` so `.planning/v1.0-MILESTONE-AUDIT.md` is regenerated from actual verification artifacts instead of hand-edited gap text. The regenerated audit must stop calling `VER-03` orphaned or missing, but it must continue to report the still-open `VER-04` and `VER-05` backfill gaps honestly. Do not “fix” the audit by deleting old prose without rerunning the workflow.</action>
  <verify>
    <automated>bash -lc 'AUDIT=.planning/v1.0-MILESTONE-AUDIT.md && rg -n "^- \\[x\\] \\*\\*VER-03\\*\\*" .planning/REQUIREMENTS.md && rg -n "^\\| VER-03 \\| Phase 6 \\| Complete \\|" .planning/REQUIREMENTS.md && rg -n "VER-03" "$AUDIT" && ! rg -n "VER-03.*orphaned|02-VERIFICATION\\.md does not exist" "$AUDIT" && rg -n "VER-04" "$AUDIT" && rg -n "VER-05" "$AUDIT"'</automated>
  </verify>
  <done>The top-level requirement table and milestone audit both recognize the new Phase 2 verification artifact, and only the still-unfinished later-phase backfills remain open.</done>
</task>

</tasks>

<verification>
Check that `VER-03` is complete in `REQUIREMENTS.md`, then confirm the regenerated milestone audit mentions `VER-03` without the old orphan/missing text and still preserves the remaining Phase 7 and 8 gap signals.
</verification>

<success_criteria>
`VER-03` is closed at the top level with truthful traceability: the requirement table is complete, the milestone audit sees the new Phase 2 verification artifact, and no unrelated gap is papered over.
</success_criteria>

<output>
After completion, create `.planning/phases/06-deterministic-runtime-verification-backfill/06-03-SUMMARY.md`
</output>
