---
phase: 04-adversarial-regression-matrix
plan: "03"
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - hsm_regression_allocs_test.go
  - muid/muid_regression_allocs_test.go
  - scripts/verify-workspace.sh
autonomous: true
requirements:
  - VER-05
must_haves:
  truths:
    - "Compatibility and performance regressions that matter to downstream users are surfaced by bounded automated evidence before release."
    - "The canonical verification script runs the new deterministic adversarial checks without becoming flaky or unbounded."
    - "Performance evidence relies on stable allocation ceilings and tiny fixed-iteration benchmark smoke instead of wall-clock thresholds."
  artifacts:
    - path: "hsm_regression_allocs_test.go"
      provides: "Stable root-package allocation regression checks for selected public hot paths"
    - path: "muid/muid_regression_allocs_test.go"
      provides: "Stable MUID allocation regression checks for default generation paths"
    - path: "scripts/verify-workspace.sh"
      provides: "Canonical gate wiring for deterministic adversarial tests, fuzz-seed replay, and benchmark smoke"
  key_links:
    - from: "scripts/verify-workspace.sh"
      to: "hsm_runtime_adversarial_test.go"
      via: "Canonical verification runs the new hostile root coverage through the existing entrypoint"
      pattern: "TestRuntimeAdversarial|TestPublicHelperAdversarial"
    - from: "scripts/verify-workspace.sh"
      to: "hsm_properties_fuzz_test.go"
      via: "Canonical verification replays checked-in fuzz seeds under normal go test"
      pattern: "FuzzHSMEventScriptProperties|FuzzMUIDGeneratorProperties"
    - from: "scripts/verify-workspace.sh"
      to: "hsm_regression_allocs_test.go"
      via: "Canonical verification executes the root allocation regression gate explicitly before benchmark smoke"
      pattern: "Test.*RegressionAllocs"
    - from: "scripts/verify-workspace.sh"
      to: "muid/muid_regression_allocs_test.go"
      via: "Canonical verification executes the MUID allocation regression gate explicitly before benchmark smoke"
      pattern: "Test.*RegressionAllocs"
    - from: "scripts/verify-workspace.sh"
      to: "hsm_bench_test.go"
      via: "Tiny fixed-iteration benchmark smoke reuses the established benchmark name"
      pattern: "BenchmarkNestedStates_NoEntryExitActivity"
    - from: "scripts/verify-workspace.sh"
      to: "muid/muid_bench_test.go"
      via: "Tiny fixed-iteration benchmark smoke reuses the established benchmark name"
      pattern: "BenchmarkMUIDGeneration"
---

<objective>
Finish Phase 4 by adding bounded compatibility and performance evidence, then wiring the canonical gate to run the full adversarial matrix.

Purpose: Adversarial coverage only matters if it shows up in the same release-blocking verifier the workspace already trusts. This plan adds CI-safe regression evidence and folds the new checks into that single canonical entrypoint.
Output: Allocation regression tests plus an updated `scripts/verify-workspace.sh` that runs deterministic adversarial suites, fuzz-seed replay, and tiny benchmark smoke.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adversarial-regression-matrix/04-RESEARCH.md
@.planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-04-SUMMARY.md
@scripts/verify-workspace.sh
@hsm_bench_test.go
@muid/muid_bench_test.go
@muid/muid.go
@hsm.go

<interfaces>
From scripts/verify-workspace.sh:

```bash
go test ./...
go test ./kind/...
go test ./muid/...
go test ./... ./kind/... ./muid/...
go test -race -short ./... ./kind/... ./muid/...
```

From the existing benchmark surface:

```go
func BenchmarkNestedStates_NoEntryExitActivity(b *testing.B)
func BenchmarkMUIDGeneration(b *testing.B)
```

From hsm.go and muid/muid.go:

```go
func Dispatch[T context.Context](ctx T, hsm Instance, event Event) <-chan struct{}
func Set[T stringLike](ctx context.Context, hsm Instance, name T, value any) <-chan struct{}
func Call[T stringLike](ctx context.Context, hsm Instance, name T, args ...any) (any, error)
func Make() MUID
func MakeString() string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stable allocation regression checks for hot public paths</name>
  <files>hsm_regression_allocs_test.go, muid/muid_regression_allocs_test.go</files>
  <behavior>
    - Selected hot public paths such as root dispatch or set flows and default MUID generation stay within explicit allocation ceilings that are stable across the pinned toolchain.
    - The regression checks avoid wall-clock thresholds and rely on `testing.AllocsPerRun` or similarly stable allocation evidence.
    - The chosen contracts stay narrow and downstream-relevant instead of trying to freeze the whole performance profile.
  </behavior>
  <action>Create focused allocation regression tests in `hsm_regression_allocs_test.go` and `muid/muid_regression_allocs_test.go` for the public paths most likely to matter to downstream users, using `testing.AllocsPerRun` with stable ceilings. Prefer zero or low-allocation contracts that are already implied by the current implementation. If a threshold is not stable enough to be a blocker, leave it out rather than forcing a flaky contract. Do not add wall-clock assertions or a broad microbenchmark harness here.</action>
  <verify>
    <automated>GOCACHE=$(pwd)/.cache/go-build go test ./... ./muid/... -run 'Test.*RegressionAllocs'</automated>
  </verify>
  <done>The workspace has CI-safe allocation regression evidence for selected root and `muid` public paths, and the checks are stable enough to block release.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the canonical verifier to run the bounded adversarial matrix</name>
  <files>scripts/verify-workspace.sh</files>
  <action>Extend `scripts/verify-workspace.sh` so the single canonical verifier remains the release gate while now also running, as explicit steps, the deterministic adversarial suites, ordinary `go test` replay of the checked-in fuzz seeds, the allocation regression tests from `hsm_regression_allocs_test.go` and `muid/muid_regression_allocs_test.go`, and a tiny fixed-iteration benchmark smoke with `-benchmem` using the existing `BenchmarkNestedStates_NoEntryExitActivity` and `BenchmarkMUIDGeneration` targets. Keep the script bounded and deterministic. Do not add open-ended `go test -fuzz`, do not create a second adversarial harness, and do not use wall-clock thresholds as pass or fail criteria.</action>
  <verify>
    <automated>GOCACHE=$(pwd)/.cache/go-build bash scripts/verify-workspace.sh</automated>
  </verify>
  <done>The canonical verifier remains the single release-blocking entrypoint and now includes deterministic adversarial, fuzz-seed, allocation, and benchmark-smoke evidence for Phase 4.</done>
</task>

</tasks>

<verification>
Phase 4 closes only on a green `GOCACHE=$(pwd)/.cache/go-build bash scripts/verify-workspace.sh`. The updated script must be bounded, deterministic, and free of open-ended fuzzing or wall-clock performance thresholds.
</verification>

<success_criteria>
The workspace now surfaces the important adversarial regression signals through one canonical gate: deterministic hostile-path tests, reproducible fuzz-seed replay, stable allocation ceilings, and tiny benchmark smoke that reuse the existing benchmark surface.
</success_criteria>

<output>
After completion, create `.planning/phases/04-adversarial-regression-matrix/04-03-SUMMARY.md`
</output>
