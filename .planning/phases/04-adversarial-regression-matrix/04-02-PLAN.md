---
phase: 04-adversarial-regression-matrix
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - hsm_properties_fuzz_test.go
  - testdata/fuzz/FuzzHSMEventScriptProperties/seed-basic-script
  - testdata/fuzz/FuzzHSMEventScriptProperties/seed-nil-and-empty
  - muid/muid_fuzz_test.go
  - muid/testdata/fuzz/FuzzMUIDGeneratorProperties/seed-default-config
  - muid/testdata/fuzz/FuzzMUIDGeneratorProperties/seed-counter-boundary
autonomous: true
requirements:
  - VER-05
must_haves:
  truths:
    - "Targeted fuzz or property-style checks preserve core HSM and MUID invariants under checked-in seed corpora."
    - "Regular `go test` execution replays the adversarial seed corpus deterministically without relying on open-ended `-fuzz` runs in the blocking path."
    - "The chosen invariants stay narrow, reproducible, and grounded in stable public contracts rather than helper-remediation or random model generation."
  artifacts:
    - path: "hsm_properties_fuzz_test.go"
      provides: "Bounded root fuzz or property target for deterministic event-script invariants"
    - path: "muid/muid_fuzz_test.go"
      provides: "Bounded MUID fuzz or property targets for monotonicity, masking, and round-trip invariants"
    - path: "testdata/fuzz/FuzzHSMEventScriptProperties/seed-basic-script"
      provides: "Checked-in root fuzz regression seed corpus"
    - path: "muid/testdata/fuzz/FuzzMUIDGeneratorProperties/seed-default-config"
      provides: "Checked-in MUID fuzz regression seed corpus"
  key_links:
    - from: "muid/muid_fuzz_test.go"
      to: "muid/muid.go"
      via: "Generator invariants exercise defaults, masking, monotonicity, and string round-trips"
      pattern: "NewGenerator|ID\\(|Make\\(|MakeString|String\\("
    - from: "testdata/fuzz/FuzzHSMEventScriptProperties/seed-basic-script"
      to: "hsm_properties_fuzz_test.go"
      via: "Normal `go test` replays the checked-in hostile seed corpus"
      pattern: "FuzzHSMEventScriptProperties"
    - from: "muid/testdata/fuzz/FuzzMUIDGeneratorProperties/seed-default-config"
      to: "muid/muid_fuzz_test.go"
      via: "Normal `go test` replays the checked-in hostile seed corpus"
      pattern: "FuzzMUIDGeneratorProperties"
---

<objective>
Add bounded fuzz and property-style regression coverage with durable seed corpora for the most valuable invariant surfaces.

Purpose: Phase 4 needs adversarial input exploration, but the canonical path must stay deterministic and reproducible. Checked-in seeds and narrow invariants provide that coverage without landing open-ended fuzzing in the default release gate.
Output: Root and `muid` fuzz or property targets plus checked-in seeds that run under ordinary `go test`.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adversarial-regression-matrix/04-RESEARCH.md
@.planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md
@.planning/phases/02-deterministic-runtime-semantics/02-01-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-03-SUMMARY.md
@hsm.go
@muid/muid.go
@muid/muid_paths_test.go
@muid/muid_test.go

<interfaces>
From hsm.go:

```go
type Event struct {
	Name   string
	ID     string
	Source string
	Target string
	Data   any
}

func Define[T stringLike](name T, redefinableElements ...RedefinableElement) Model
func Started[T Instance](ctx context.Context, sm T, model *Model, maybeConfig ...Config) T
func Dispatch[T context.Context](ctx T, hsm Instance, event Event) <-chan struct{}
func AfterProcess(ctx context.Context, hsm Instance, maybeEvent ...Event) <-chan struct{}
```

From muid/muid.go:

```go
type Config struct {
	MachineID       uint64
	TimestampBitLen int
	MachineIDBitLen int
	Epoch           int64
}

type MUID uint64

func (m MUID) String() string
func NewGenerator(config Config, shardIndex uint64, shardBitLen int) *Generator
func (g *Generator) ID() MUID
func Make() MUID
func MakeString() string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a bounded root property target with checked-in hostile event seeds</name>
  <files>hsm_properties_fuzz_test.go, testdata/fuzz/FuzzHSMEventScriptProperties/seed-basic-script, testdata/fuzz/FuzzHSMEventScriptProperties/seed-nil-and-empty</files>
  <behavior>
    - Seed-backed root inputs preserve core invariants such as no stuck processing waiter, no duplicate terminal processing for a tiny deterministic event-script surface, and stable settled state after replay.
    - The root property target uses checked-in seeds that run under ordinary `go test` and remain reproducible when a seed fails.
    - The target stays narrow and deterministic; it does not generate arbitrary models, depend on nil-helper characterization, or rely on open-ended `go test -fuzz` in the blocking path.
  </behavior>
  <action>Create `hsm_properties_fuzz_test.go` with one bounded root fuzz or property target focused on a tiny deterministic event-script surface that exercises already-stable public runtime behavior instead of helper-remediation seams or random model generation. Check in a small seed corpus under `testdata/fuzz/FuzzHSMEventScriptProperties/` that includes at least one normal script and one malformed-but-supported event-script shape such as empty names or duplicate sequences, but not nil-helper or runtime-fault cases that belong to 04-01. Keep the assertions about invariants and settled behavior, not about scheduler order or exploratory throughput.</action>
  <verify>
    <automated>GOCACHE=$(pwd)/.cache/go-build go test ./... -run 'FuzzHSMEventScriptProperties'</automated>
  </verify>
  <done>The root package has a deterministic property target backed by checked-in seeds for stable public event-script invariants, and those seeds replay under ordinary `go test` without an explicit fuzz campaign or any dependency on 04-01 remediation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MUID generator property targets and durable seed corpus</name>
  <files>muid/muid_fuzz_test.go, muid/testdata/fuzz/FuzzMUIDGeneratorProperties/seed-default-config, muid/testdata/fuzz/FuzzMUIDGeneratorProperties/seed-counter-boundary</files>
  <behavior>
    - Seed-backed MUID inputs preserve defaults, machine-ID masking, monotonic ID ordering, and string round-trip invariants across targeted hostile configurations.
    - The MUID property target exercises checked-in seeds during normal `go test` and remains fast enough for the canonical gate.
    - Open-ended `go test -fuzz` remains optional and bounded outside the default blocking path.
  </behavior>
  <action>Create `muid/muid_fuzz_test.go` with targeted fuzz or property-style coverage for generator configuration and emitted IDs, reusing the invariants already established in `muid_paths_test.go` and `muid_test.go`. Check in a seed corpus under `muid/testdata/fuzz/FuzzMUIDGeneratorProperties/` that includes a default-like configuration and a counter or timestamp boundary case. Preserve the current package contract and avoid introducing third-party property libraries or long-running fuzz loops.</action>
  <verify>
    <automated>GOCACHE=$(pwd)/.cache/go-build go test ./muid/... -run 'FuzzMUIDGeneratorProperties'</automated>
  </verify>
  <done>`muid` has bounded property coverage with checked-in seeds for its highest-value invariants, and those seeds replay deterministically under normal test execution.</done>
</task>

</tasks>

<verification>
Use ordinary `go test` to replay the fuzz seeds: `GOCACHE=$(pwd)/.cache/go-build go test ./... ./muid/... -run 'FuzzHSMEventScriptProperties|FuzzMUIDGeneratorProperties'`. If deeper exploration is useful, document a separate bounded opt-in command with `-fuzztime`, but do not add it to the default blocking path.
</verification>

<success_criteria>
Phase 4 now has checked-in adversarial seed corpora and narrow property targets for the root runtime and `muid`, and the default test path replays those seeds deterministically without turning the canonical gate into an unbounded fuzz job.
</success_criteria>

<output>
After completion, create `.planning/phases/04-adversarial-regression-matrix/04-02-SUMMARY.md`
</output>
