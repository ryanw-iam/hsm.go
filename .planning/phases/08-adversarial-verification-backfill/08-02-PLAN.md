---
phase: 08-adversarial-verification-backfill
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md
  - .planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md
autonomous: true
requirements:
  - VER-05
gap_closure: true
must_haves:
  truths:
    - "Phase 4 has a milestone-grade `04-VERIFICATION.md` grounded in fresh command evidence against current code."
    - "`04-VALIDATION.md` no longer reads as draft or pending-only audit debt once the backfill is complete."
    - "The verification report and validation record cite the same `GOCACHE=\"$PWD/.cache/go-build\"` command family, so checker traceability does not rely on inference."
  artifacts:
    - path: ".planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md"
      provides: "Goal-backward verification report for the shipped Phase 4 adversarial scope"
    - path: ".planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md"
      provides: "Reconciled executed validation record with copy-safe commands and completed statuses"
  key_links:
    - from: ".planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md"
      to: ".planning/phases/04-adversarial-regression-matrix/04-01-SUMMARY.md"
      via: "Runtime adversarial and helper-contract claims must map back to the shipped Phase 4 runtime evidence"
      pattern: "TestRuntimeAdversarial|TestPublicHelperAdversarial|typed-nil|panic"
    - from: ".planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md"
      to: ".planning/phases/04-adversarial-regression-matrix/04-02-SUMMARY.md"
      via: "Fuzz-seed and property claims must map back to the shipped root and MUID corpus evidence"
      pattern: "FuzzHSMEventScriptProperties|FuzzMUIDGeneratorProperties|seed"
    - from: ".planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md"
      to: ".planning/phases/04-adversarial-regression-matrix/04-03-SUMMARY.md"
      via: "Allocation and benchmark-smoke claims must map back to the shipped closeout evidence"
      pattern: "Test.*RegressionAllocs|BenchmarkNestedStates_NoEntryExitActivity|BenchmarkMUIDGeneration"
    - from: ".planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md"
      to: "scripts/verify-workspace.sh"
      via: "The reconciled validation record must preserve the same canonical gate used by the verification report"
      pattern: "GOCACHE=.*bash scripts/verify-workspace\\.sh"
---

<objective>
Backfill the missing Phase 4 verification artifacts from the fresh adversarial evidence produced in Plan 08-01.

Purpose: The audit orphan exists because Phase 4 has shipped summaries but no milestone-grade verification artifact, and its validation doc still presents draft intent instead of executed proof.
Output: A truthful `04-VERIFICATION.md` plus a reconciled `04-VALIDATION.md` that both point at the same executed commands.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-adversarial-verification-backfill/08-RESEARCH.md
@.planning/phases/08-adversarial-verification-backfill/08-VALIDATION.md
@.planning/phases/08-adversarial-verification-backfill/08-01-SUMMARY.md
@.planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md
@.planning/phases/04-adversarial-regression-matrix/04-01-SUMMARY.md
@.planning/phases/04-adversarial-regression-matrix/04-02-SUMMARY.md
@.planning/phases/04-adversarial-regression-matrix/04-03-SUMMARY.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-VERIFICATION.md
@.planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md
@README.md
@scripts/verify-workspace.sh

<interfaces>
From .planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md:

```text
Observable Truths
Required Artifacts
Key Link Verification
Requirements Coverage
```

From .planning/phases/08-adversarial-verification-backfill/08-VALIDATION.md:

```text
GOCACHE="$PWD/.cache/go-build" go test ./... ./muid/... -run 'TestRuntimeAdversarial|TestPublicHelperAdversarial|FuzzHSMEventScriptProperties|FuzzMUIDGeneratorProperties|Test.*RegressionAllocs' -count=1
GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write a milestone-grade `04-VERIFICATION.md` from fresh adversarial evidence</name>
  <files>.planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md</files>
  <action>Create `.planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md` in the same verification-report shape used by the completed phase artifacts, but scoped strictly to the shipped Phase 4 adversarial surface. Derive truths from the Phase 4 roadmap success criteria, the three Phase 4 summaries, the actual adversarial/fuzz/allocation files, the canonical verification script, and the fresh commands from Plan 08-01. Explicitly call out any tiny remediation that Plan 08-01 had to make. Do not write placeholder language, future tense, or copied audit prose; the report must read like a completed verification artifact, not a plan.</action>
  <verify>
    <automated>bash -lc 'REPORT=.planning/phases/04-adversarial-regression-matrix/04-VERIFICATION.md && test -f "$REPORT" && rg -n "^# Phase 4: Adversarial Regression Matrix Verification Report$" "$REPORT" && rg -n "^\\*\\*Status:\\*\\* passed$" "$REPORT" && rg -n "^### Observable Truths$" "$REPORT" && rg -n "^### Required Artifacts$" "$REPORT" && rg -n "^### Key Link Verification$" "$REPORT" && rg -n "^## Requirements Coverage$" "$REPORT" && rg -n "VER-05" "$REPORT" && rg -n "hsm_runtime_adversarial_test\\.go|hsm_properties_fuzz_test\\.go|muid/muid_fuzz_test\\.go|hsm_regression_allocs_test\\.go|muid/muid_regression_allocs_test\\.go" "$REPORT" && rg -n "scripts/verify-workspace\\.sh|BenchmarkNestedStates_NoEntryExitActivity|BenchmarkMUIDGeneration" "$REPORT"'</automated>
  </verify>
  <done>`04-VERIFICATION.md` exists, reads like a completed verification report, and ties `VER-05` to concrete adversarial, fuzz-seed, allocation, benchmark, and gate evidence from the current codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile `04-VALIDATION.md` from draft intent to executed evidence</name>
  <files>.planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md</files>
  <action>Update `04-VALIDATION.md` so it reflects executed backfill reality instead of draft intent: preserve the original six Phase 4 task rows, add `-count=1` where needed for copy-safe reruns, change task rows from `⬜ pending` to completed status values consistent with the executed evidence, and move the file out of draft and pending sign-off language. Keep the validation map aligned to the shipped Phase 4 execution shape plus the Phase 8 backfill commands; do not invent Phase 8-only rows, manual verification requirements, or new adversarial categories.</action>
  <verify>
    <automated>bash -lc 'VALIDATION=.planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md && ! rg -n "^status: draft|⬜ pending|Approval: pending" "$VALIDATION" && rg -n "04-01-01|04-01-02|04-02-01|04-02-02|04-03-01|04-03-02" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" go test ./... -run '\''TestRuntimeStopTimeoutDispatchesErrorEventDeterministically\\|TestRuntimeRulesConformance'\''" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" go test ./... -run '\''TestRuntimeAdversarial\\|TestPublicHelperAdversarial'\'' -count=1" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" go test ./... -run '\''FuzzHSMEventScriptProperties'\'' -count=1" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" go test ./muid/... -run '\''FuzzMUIDGeneratorProperties'\'' -count=1" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" bash scripts/verify-workspace\\.sh" "$VALIDATION"'</automated>
  </verify>
  <done>`04-VALIDATION.md` no longer communicates draft or pending debt and records the same executable commands used to backfill Phase 4 evidence.</done>
</task>

</tasks>

<verification>
Confirm `04-VERIFICATION.md` exists with the standard verification-report sections, then confirm `04-VALIDATION.md` no longer contains draft or pending markers and uses the same copy-safe command family that actually proved Phase 4.
</verification>

<success_criteria>
Phase 4 now has both a milestone-grade verification report and a reconciled validation record, and the two artifacts agree on what was executed to satisfy `VER-05`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-adversarial-verification-backfill/08-02-SUMMARY.md`
</output>
