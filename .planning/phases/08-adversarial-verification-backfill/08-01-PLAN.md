---
phase: 08-adversarial-verification-backfill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hsm.go
  - hsm_runtime_adversarial_test.go
  - hsm_properties_fuzz_test.go
  - hsm_regression_allocs_test.go
  - muid/muid_fuzz_test.go
  - muid/muid_regression_allocs_test.go
  - scripts/verify-workspace.sh
autonomous: true
requirements:
  - VER-05
gap_closure: true
must_haves:
  truths:
    - "The shipped Phase 4 adversarial claims are re-proven against current code instead of being trusted from old summaries alone."
    - "Any failure uncovered during re-verification is remediated only inside the existing Phase 4 adversarial slice, and only if that tiny fix is required to keep the evidence truthful."
    - "The focused adversarial rerun and the canonical workspace gate both pass before any Phase 8 document claims `VER-05` closure."
  artifacts:
    - path: "hsm_runtime_adversarial_test.go"
      provides: "Focused runtime adversarial coverage for panic recovery, timeout, and hostile helper contracts"
    - path: "hsm_properties_fuzz_test.go"
      provides: "Focused root fuzz-seed regression carrier for deterministic event-script invariants"
    - path: "muid/muid_fuzz_test.go"
      provides: "Focused MUID fuzz-seed regression carrier for generator and round-trip invariants"
    - path: "hsm_regression_allocs_test.go"
      provides: "Stable root-package allocation regression coverage for shipped hot paths"
    - path: "muid/muid_regression_allocs_test.go"
      provides: "Stable MUID allocation regression coverage for shipped generation paths"
    - path: "scripts/verify-workspace.sh"
      provides: "Canonical workspace gate that already exercises the bounded adversarial matrix"
  key_links:
    - from: "hsm_runtime_adversarial_test.go"
      to: "hsm.go"
      via: "Runtime fault and helper-contract tests must still match current panic-recovery and nil-safe helper semantics"
      pattern: "TestRuntimeAdversarial|TestPublicHelperAdversarial|Dispatch\\(|Set\\(|Stop\\(|ErrorEvent"
    - from: "hsm_properties_fuzz_test.go"
      to: "scripts/verify-workspace.sh"
      via: "Root fuzz-seed replay must remain part of ordinary canonical verification"
      pattern: "FuzzHSMEventScriptProperties|go test ./\\.\\.\\."
    - from: "muid/muid_fuzz_test.go"
      to: "scripts/verify-workspace.sh"
      via: "MUID fuzz-seed replay must remain part of ordinary canonical verification"
      pattern: "FuzzMUIDGeneratorProperties|go test ./muid/\\.\\.\\."
    - from: "hsm_regression_allocs_test.go"
      to: "scripts/verify-workspace.sh"
      via: "Allocation regression checks must remain wired into the release gate"
      pattern: "Test.*RegressionAllocs|bench"
    - from: "muid/muid_regression_allocs_test.go"
      to: "scripts/verify-workspace.sh"
      via: "MUID allocation regression and benchmark smoke must survive the canonical gate used for shipping"
      pattern: "Test.*RegressionAllocs|BenchmarkMUIDGeneration"
---

<objective>
Re-prove the shipped Phase 4 adversarial slice before any backfill document claims `VER-05` closure.

Purpose: Phase 8 closes an audit orphan, not unfinished Phase 4 feature work. This plan verifies the current truth first so later evidence stays honest.
Output: Green focused adversarial verification on current code, plus only a tiny Phase 4 correction if fresh evidence disproves an existing claim.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-adversarial-verification-backfill/08-RESEARCH.md
@.planning/phases/08-adversarial-verification-backfill/08-VALIDATION.md
@.planning/phases/04-adversarial-regression-matrix/04-RESEARCH.md
@.planning/phases/04-adversarial-regression-matrix/04-VALIDATION.md
@.planning/phases/04-adversarial-regression-matrix/04-01-SUMMARY.md
@.planning/phases/04-adversarial-regression-matrix/04-02-SUMMARY.md
@.planning/phases/04-adversarial-regression-matrix/04-03-SUMMARY.md
@README.md
@hsm.go
@hsm_runtime_adversarial_test.go
@hsm_properties_fuzz_test.go
@hsm_regression_allocs_test.go
@muid/muid_fuzz_test.go
@muid/muid_regression_allocs_test.go
@scripts/verify-workspace.sh

<interfaces>
From .planning/phases/08-adversarial-verification-backfill/08-VALIDATION.md:

```text
GOCACHE="$PWD/.cache/go-build" go test ./... ./muid/... -run 'TestRuntimeAdversarial|TestPublicHelperAdversarial|FuzzHSMEventScriptProperties|FuzzMUIDGeneratorProperties|Test.*RegressionAllocs' -count=1
GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh
```

From .planning/phases/04-adversarial-regression-matrix/04-03-SUMMARY.md:

```text
The canonical verifier remains the single release-blocking entrypoint and now includes deterministic adversarial, fuzz-seed, allocation, and benchmark-smoke evidence for Phase 4.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the shipped adversarial slice and keep any correction microscopic</name>
  <files>hsm.go, hsm_runtime_adversarial_test.go, hsm_properties_fuzz_test.go, hsm_regression_allocs_test.go, muid/muid_fuzz_test.go, muid/muid_regression_allocs_test.go, scripts/verify-workspace.sh</files>
  <action>Run `GOCACHE="$PWD/.cache/go-build" go test ./... ./muid/... -run 'TestRuntimeAdversarial|TestPublicHelperAdversarial|FuzzHSMEventScriptProperties|FuzzMUIDGeneratorProperties|Test.*RegressionAllocs' -count=1`, then run `GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh`. If both commands pass, leave the Phase 4 adversarial slice unchanged and capture the exact command and output facts for Plan 08-02. If re-verification fails because the current code disproves a shipped Phase 4 claim, fix only the smallest blocking issue inside the existing adversarial slice listed in this plan. Keep remediation limited to current runtime-adversarial, fuzz-seed, allocation-regression, or canonical-gate seams; do not add new adversarial families, broaden benchmark scope, or reopen later milestone-closeout work. If a truthful fix would require broader scope than these files, stop and surface that mismatch instead of widening the plan.</action>
  <verify>
    <automated>bash -lc 'GOCACHE="$PWD/.cache/go-build" go test ./... ./muid/... -run '\''TestRuntimeAdversarial|TestPublicHelperAdversarial|FuzzHSMEventScriptProperties|FuzzMUIDGeneratorProperties|Test.*RegressionAllocs'\'' -count=1 && GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh'</automated>
  </verify>
  <done>The current repository truth for Phase 4 is known from fresh command evidence, and any required correction stays microscopic inside the shipped adversarial slice.</done>
</task>

</tasks>

<verification>
Run the focused adversarial command from `08-VALIDATION.md`, then `GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh`. No later Phase 8 document should claim `VER-05` closure unless both commands are green on the same workspace state.
</verification>

<success_criteria>
Phase 8 has fresh executable proof for the current Phase 4 adversarial slice, and any issue uncovered during that proof is either fixed only inside the shipped adversarial seam or explicitly surfaced as out of scope for this backfill.
</success_criteria>

<output>
After completion, create `.planning/phases/08-adversarial-verification-backfill/08-01-SUMMARY.md`
</output>
