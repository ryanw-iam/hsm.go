---
phase: 03-rules-conformance-matrix
plan: "04"
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - .planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md
autonomous: true
requirements:
  - VER-04
must_haves:
  truths:
    - "The checked-in rules matrix points to actual model and runtime rule-ID evidence, not placeholders."
    - "All Phase 3 conformance evidence is exercised by the canonical workspace verification gate."
    - "The final Phase 3 artifact set makes it obvious which rules are negative checks versus runtime semantics versus positive exemplars."
  artifacts:
    - path: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      provides: "Finalized rule matrix reconciled to actual conformance suites and evidence names"
  key_links:
    - from: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      to: "hsm_rules_model_conformance_test.go"
      via: "Actual `define_panic` evidence references"
      pattern: "TestModelRulesConformance"
    - from: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      to: "hsm_rules_runtime_conformance_test.go"
      via: "Actual `runtime_semantic` and `exemplar` evidence references"
      pattern: "TestRuntimeRulesConformance"
    - from: "scripts/verify-workspace.sh"
      to: "hsm_rules_model_conformance_test.go"
      via: "Canonical workspace gate runs root-package tests"
      pattern: "go test ./..."
---

<objective>
Close Phase 3 by reconciling the matrix to the implemented evidence and proving the full conformance set under the canonical gate.

Purpose: The phase is not actually release-gated until the checked-in matrix points at real suites and the canonical verifier runs after both model and runtime conformance plans complete.
Output: A finalized `03-RULES-MATRIX.md` plus a green `bash scripts/verify-workspace.sh` run after all Phase 3 suites land.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rules-conformance-matrix/03-RESEARCH.md
@.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md
@rules.md
@hsm_rules_model_conformance_test.go
@hsm_rules_runtime_conformance_test.go
@scripts/verify-workspace.sh

<interfaces>
From scripts/verify-workspace.sh:

```bash
go test ./...
go test ./kind/...
go test ./muid/...
go test ./... ./kind/... ./muid/...
go test -race -short ./... ./kind/... ./muid/...
```

From the Phase 3 contract:

```text
03-RULES-MATRIX.md must identify whether each HSMxx rule is a define_panic, runtime_semantic, or exemplar case and point to actual dedicated-suite evidence.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize matrix evidence and prove the canonical rules gate</name>
  <files>.planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md</files>
  <action>Reconcile `03-RULES-MATRIX.md` against the implemented `hsm_rules_model_conformance_test.go` and `hsm_rules_runtime_conformance_test.go` suites so the matrix points to actual subtest names rather than planned placeholders. Keep the same three enforcement classes and make any necessary naming cleanup limited to matrix/evidence alignment. After the matrix is finalized, run `bash scripts/verify-workspace.sh` so Phase 3 closes only after both dedicated conformance suites are included in the canonical workspace gate. Do not add a second verifier, do not introduce Phase 4 fuzzing or Phase 5 remediation work here, and do not change `rules.md` unless a wording ambiguity literally blocks matrix-to-test traceability.</action>
  <verify>
    <automated>bash scripts/verify-workspace.sh</automated>
  </verify>
  <done>The matrix references actual model and runtime rule-ID evidence, and the canonical workspace gate passes after the full Phase 3 conformance set is in place.</done>
</task>

</tasks>

<verification>
Phase 3 closes only on a green `bash scripts/verify-workspace.sh` after both dedicated conformance suites are finished and the checked-in matrix references their actual evidence.
</verification>

<success_criteria>
The final Phase 3 artifact set is internally consistent: `rules.md` is mapped by a checked-in matrix to real rule-ID tests, the matrix distinguishes hard-negative checks from runtime semantics and exemplars, and the canonical workspace gate verifies all of it together.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rules-conformance-matrix/03-04-SUMMARY.md`
</output>
