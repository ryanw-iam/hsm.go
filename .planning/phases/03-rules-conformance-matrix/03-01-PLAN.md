---
phase: 03-rules-conformance-matrix
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md
  - hsm_test_helpers_test.go
autonomous: true
requirements:
  - VER-04
must_haves:
  truths:
    - "Every HSM rule in `rules.md` is mapped to an honest enforcement class and a concrete evidence target."
    - "Phase 3 has shared conformance helpers for stable panic-substring assertions and deterministic waiter usage."
    - "Downstream plans can implement model and runtime rule suites without re-classifying the contract."
  artifacts:
    - path: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      provides: "Checked-in rule matrix covering HSM01-HSM54 with enforcement class, evidence target, and notes"
    - path: "hsm_test_helpers_test.go"
      provides: "Shared panic-message and waiter helpers used by dedicated conformance suites"
  key_links:
    - from: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      to: "rules.md"
      via: "One row per HSM rule with enforcement-class mapping"
      pattern: "HSM(0[1-9]|[1-4][0-9]|5[0-4])"
    - from: "hsm_test_helpers_test.go"
      to: "hsm_rules_model_conformance_test.go"
      via: "Stable panic assertion helper"
      pattern: "assertPanicContains"
    - from: "hsm_test_helpers_test.go"
      to: "hsm_rules_runtime_conformance_test.go"
      via: "Deterministic waiter helpers"
      pattern: "awaitWaiter|assertWaiterPending"
---

<objective>
Create the honest conformance inventory and shared helper contract that Phase 3 execution will build against.

Purpose: Phase 3 succeeds only if every `rules.md` rule is classified by what the library can actually enforce today and the later suites share the same stable helper surface.
Output: A checked-in rules matrix plus reusable root test helpers for model and runtime conformance suites.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rules-conformance-matrix/03-RESEARCH.md
@.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-03-SUMMARY.md
@.planning/phases/02-deterministic-runtime-semantics/02-01-SUMMARY.md
@.planning/phases/02-deterministic-runtime-semantics/02-02-SUMMARY.md
@rules.md
@hsm_test_helpers_test.go

<interfaces>
From hsm_test_helpers_test.go:

```go
func awaitWaiter(t *testing.T, description string, waiter <-chan struct{})
func assertWaiterPending(t *testing.T, description string, waiter <-chan struct{})
func assertPanic(t *testing.T, name string, fn func())
func assertNoPanic(t *testing.T, name string, fn func())
```

Use these existing helpers as the base contract. Extend them only where the conformance suites need a stable panic-substring assertion; do not introduce a third-party assertion library or sleep-oriented helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the checked-in rules conformance matrix</name>
  <files>.planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md</files>
  <action>Create `03-RULES-MATRIX.md` as the executable inventory for Phase 3 using a markdown table with stable columns in this order: `Rule | Enforcement | Evidence | Notes`. Cover every `HSM01` through `HSM54` rule exactly once, preserving `rules.md` wording as the source contract while adding an honest `enforcement` classification of `define_panic`, `runtime_semantic`, or `exemplar`. For each row, record the evidence target using the dedicated suite and subtest name that later plans must create, plus a short note explaining why the rule belongs in that class. Reuse existing evidence only when it is already specific and stable enough; otherwise point the row at the new dedicated suites. Do not rewrite `rules.md`, invent extra rule categories, or backfill Phase 4/5 work into this matrix.</action>
  <verify>
    <automated>test -f .planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md && diff -u &lt;(sed -n 's/^- \\(HSM[0-9][0-9]\\):.*/\\1/p' rules.md | sort) &lt;(awk -F'|' '/^\\| HSM[0-9][0-9] / {gsub(/ /, "", $2); print $2}' .planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md | sort) && awk -F'|' '/^\\| HSM[0-9][0-9] / {gsub(/ /, "", $3); if ($3 != "define_panic" &amp;&amp; $3 != "runtime_semantic" &amp;&amp; $3 != "exemplar") exit 1} END {exit 0}' .planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md</automated>
  </verify>
  <done>`03-RULES-MATRIX.md` exists, lists every rule, and tells downstream plans which rules are hard-negative checks versus deterministic runtime semantics versus positive exemplars.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared helpers for stable conformance assertions</name>
  <files>hsm_test_helpers_test.go</files>
  <action>Extend the root test helper file with only the small shared affordances needed by the dedicated conformance suites, especially a panic helper that asserts stable message fragments instead of exact traceback strings. Keep the helpers generic, rooted in existing repository style, and compatible with Phase 2 waiter-based determinism. Do not add a custom DSL, a third-party assertion library, or any helper that depends on `time.Sleep` to make runtime rules pass.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <done>The root helper surface supports substring panic assertions and deterministic waiter usage for both future conformance suites without changing the release-gate model.</done>
</task>

</tasks>

<verification>
Task 1 must leave a reviewable matrix artifact in-repo. Task 2 must finish on a green `go test ./...` so Wave 2 can build new suites in parallel against a stable helper contract.
</verification>

<success_criteria>
Phase 3 now has one checked-in source of conformance truth beyond `rules.md`: every rule is classified honestly, mapped to a concrete evidence target, and backed by shared helpers that keep later tests deterministic and stable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rules-conformance-matrix/03-01-SUMMARY.md`
</output>
