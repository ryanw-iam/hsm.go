---
phase: 03-rules-conformance-matrix
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - hsm_rules_model_conformance_test.go
autonomous: true
requirements:
  - VER-04
must_haves:
  truths:
    - "Invalid model definitions covered by build-time rules fail with repeatable rule-ID evidence."
    - "Define-time rule failures can be targeted directly with `go test -run TestModelRulesConformance/...`."
    - "The model conformance suite uses stable panic-fragment assertions rather than brittle full traceback matching."
  artifacts:
    - path: "hsm_rules_model_conformance_test.go"
      provides: "Dedicated define/build-time conformance suite for `define_panic` rules"
  key_links:
    - from: "hsm_rules_model_conformance_test.go"
      to: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      via: "Each `define_panic` row maps to a matching rule-ID subtest"
      pattern: "define_panic"
    - from: "hsm_rules_model_conformance_test.go"
      to: "hsm_test_helpers_test.go"
      via: "Shared panic-substring assertion helper"
      pattern: "assertPanicContains"
    - from: "hsm_rules_model_conformance_test.go"
      to: "hsm.go"
      via: "Model builders and validation panics"
      pattern: "Define\\(|Initial\\(|Choice\\(|Final\\(|OnSet\\(|OnCall\\("
---

<objective>
Turn the matrix rows classified as build-time enforcement into a dedicated model conformance suite.

Purpose: `VER-04` requires invalid model definitions to fail in clear, repeatable ways instead of remaining scattered across ad hoc tests.
Output: A root `TestModelRulesConformance` suite with rule-ID subtests for every `define_panic` matrix row.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rules-conformance-matrix/03-RESEARCH.md
@.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md
@rules.md
@hsm.go
@hsm_test_helpers_test.go

<interfaces>
From hsm_test_helpers_test.go:

```go
func assertPanic(t *testing.T, name string, fn func())
func assertNoPanic(t *testing.T, name string, fn func())
// Plan 03-01 extends this file with a panic-fragment helper for stable assertions.
```

From hsm.go:

```go
func Define[T Context](name stringLike, elements ...RedefinableElement) Model
func State(name stringLike, elements ...RedefinableElement) *state
func Transition(elements ...RedefinableElement) *transition
func Initial(elements ...RedefinableElement) *initial
func Choice(name stringLike, transitions ...RedefinableElement) *choice
func Final(name stringLike, elements ...RedefinableElement) *state
func OnSet(name string) *triggerDef
func OnCall(name string) *triggerDef
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the define-time rules conformance suite</name>
  <files>hsm_rules_model_conformance_test.go</files>
  <behavior>
    - Test failing models with missing or invalid initial-state structure such as HSM01, HSM02, HSM05, HSM06, and HSM07.
    - Test ownership and declaration-shape violations such as HSM08, HSM09, HSM20, HSM21, HSM32, HSM33, and HSM34.
    - Test structural vertex rules such as HSM16, HSM17, HSM18, HSM19, HSM42, HSM49, and HSM50 when the library rejects them during model construction.
    - Shared scenarios may cover tightly coupled rules only if each satisfied rule ID is explicit in the matrix and subtest naming.
  </behavior>
  <action>Create `hsm_rules_model_conformance_test.go` with one top-level `TestModelRulesConformance` function and rule-ID subtests sourced from the `define_panic` rows in `03-RULES-MATRIX.md`. Use table-driven cases where practical, assert stable panic substrings instead of full traceback strings, and keep the suite focused on model-definition/build-time rejection only. Reuse existing feature coverage ideas when helpful, but do not leave the conformance evidence buried in `hsm_test.go`, and do not drift into runtime-settled semantics or positive exemplars here.</action>
  <verify>
    <automated>go test ./... -run 'TestModelRulesConformance'</automated>
  </verify>
  <done>Every matrix row marked `define_panic` is represented by a dedicated model conformance subtest, and invalid models fail with stable, reviewable rule-specific evidence.</done>
</task>

</tasks>

<verification>
The targeted model suite must pass with `go test ./... -run 'TestModelRulesConformance'` and provide direct rule-ID targeting for build-time contract enforcement.
</verification>

<success_criteria>
All build-time-enforceable rules in `rules.md` are locked behind a dedicated conformance suite, using stable panic-fragment assertions and rule-ID subtests that match the checked-in matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rules-conformance-matrix/03-02-SUMMARY.md`
</output>
