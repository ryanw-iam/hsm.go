---
phase: 03-rules-conformance-matrix
plan: "03"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - hsm_rules_runtime_conformance_test.go
autonomous: true
requirements:
  - VER-04
must_haves:
  truths:
    - "Runtime semantic rules and exemplar-only guidance are represented by deterministic, reviewable rule-ID conformance tests."
    - "Positive-only exemplar rules stay labeled honestly instead of pretending the library can reject misuse automatically."
    - "The runtime conformance suite can be targeted directly and run independently from the model conformance suite."
  artifacts:
    - path: "hsm_rules_runtime_conformance_test.go"
      provides: "Dedicated runtime/exemplar conformance suite with waiter-based deterministic assertions"
  key_links:
    - from: "hsm_rules_runtime_conformance_test.go"
      to: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      via: "Each `runtime_semantic` or `exemplar` row maps to a matching rule-ID subtest"
      pattern: "runtime_semantic|exemplar"
    - from: "hsm_rules_runtime_conformance_test.go"
      to: "hsm_test_helpers_test.go"
      via: "Waiter-based deterministic assertions"
      pattern: "awaitWaiter|assertWaiterPending"
---

<objective>
Turn the runtime and exemplar matrix rows into deterministic conformance evidence.

Purpose: Phase 3 needs explicit runtime and advisory-rule evidence that is reviewable on its own before the phase closeout gate reconciles everything together.
Output: A dedicated runtime/exemplar conformance suite with rule-ID subtests and deterministic assertions.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rules-conformance-matrix/03-RESEARCH.md
@.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md
@.planning/phases/02-deterministic-runtime-semantics/02-01-SUMMARY.md
@.planning/phases/02-deterministic-runtime-semantics/02-02-SUMMARY.md
@.planning/phases/02-deterministic-runtime-semantics/02-03-SUMMARY.md
@rules.md
@hsm.go
@hsm_test_helpers_test.go

<interfaces>
From hsm_test_helpers_test.go:

```go
func awaitWaiter(t *testing.T, description string, waiter <-chan struct{})
func assertWaiterPending(t *testing.T, description string, waiter <-chan struct{})
```

From hsm.go:

```go
func Dispatch(ctx context.Context, hsm Instance, event Event) <-chan struct{}
func Set(ctx context.Context, hsm Instance, name string, value any) <-chan struct{}
func Call(ctx context.Context, hsm Instance, name string, args ...any) (any, error)
func Restart(ctx context.Context, hsm Instance, maybeData ...any) <-chan struct{}
func Stop(ctx context.Context, hsm Instance) <-chan struct{}
func AfterEntry[T stringLike](ctx context.Context, hsm Instance, state T) <-chan struct{}
func AfterProcess[T stringLike](ctx context.Context, hsm Instance, event T) <-chan struct{}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the runtime and exemplar rules conformance suite</name>
  <files>hsm_rules_runtime_conformance_test.go</files>
  <behavior>
    - Test deterministic runtime semantics such as HSM13-HSM15, HSM29-HSM31, HSM35-HSM46, HSM48, and HSM52-HSM54 with waiter-based settled-state assertions.
    - Cover rules like HSM22-HSM28, HSM36-HSM41, HSM44-HSM47, and HSM51 as positive exemplars where the library cannot reject misuse directly today.
    - Ensure shared scenarios identify every covered HSM ID explicitly in the matrix and test naming when one runtime flow proves multiple closely related rules.
  </behavior>
  <action>Create `hsm_rules_runtime_conformance_test.go` with one top-level `TestRuntimeRulesConformance` function and rule-ID subtests for every `runtime_semantic` and `exemplar` row in `03-RULES-MATRIX.md`. Reuse the deterministic patterns established in Phase 2: wait on returned channels, use waiter helpers instead of sleeps, and assert stable settled state or trace outcomes. For exemplar-only rules, write positive usage scenarios and label them honestly; do not fake impossible negative enforcement. Keep this suite centered on `rules.md` only and do not expand into Phase 4 fuzzing, fault injection, or broad remediation work.</action>
  <verify>
    <automated>go test ./... -run 'TestRuntimeRulesConformance'</automated>
  </verify>
  <done>The runtime/exemplar suite covers every non-`define_panic` matrix row with deterministic rule-ID evidence, and exemplar rules are represented as positive usage tests only.</done>
</task>

</tasks>

<verification>
Run the runtime suite in isolation with `go test ./... -run 'TestRuntimeRulesConformance'` so the executor can debug runtime and exemplar rule evidence without waiting on the full workspace gate.
</verification>

<success_criteria>
All runtime-semantic and exemplar rules from `rules.md` have deterministic executable evidence, exemplar-only rules remain honestly classified, and the suite is ready for the final phase closeout gate.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rules-conformance-matrix/03-03-SUMMARY.md`
</output>
