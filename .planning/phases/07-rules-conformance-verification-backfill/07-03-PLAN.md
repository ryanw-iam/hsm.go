---
phase: 07-rules-conformance-verification-backfill
plan: "03"
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - VER-04
gap_closure: true
must_haves:
  truths:
    - "`REQUIREMENTS.md` no longer leaves `VER-04` pending once the backfilled Phase 3 verification artifact exists."
    - "The milestone audit stops reporting Phase 3 or `VER-04` as orphaned because it can now see the real Phase 3 verification artifact."
    - "Traceability closure for `VER-04` does not hide the still-open Phase 8 and `VER-05` audit debt."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Top-level requirement traceability showing `VER-04` complete through the Phase 7 backfill"
    - path: ".planning/v1.0-MILESTONE-AUDIT.md"
      provides: "Regenerated audit report that reflects the closed Phase 3 orphan without hand-edited fiction"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md"
      via: "Requirement closure must be backed by the real Phase 3 verification artifact, not summary frontmatter alone"
      pattern: "VER-04"
    - from: ".planning/v1.0-MILESTONE-AUDIT.md"
      to: ".planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md"
      via: "The audit should stop flagging the missing Phase 3 verification report once the artifact exists"
      pattern: "03-VERIFICATION\\.md"
    - from: ".planning/v1.0-MILESTONE-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "The regenerated audit must still preserve the remaining `VER-05` gap and later backfill debt"
      pattern: "VER-05|gaps_found"
---

<objective>
Close the top-level `VER-04` traceability gap without hand-waving away the remaining milestone audit debt.

Purpose: The backfilled Phase 3 evidence only matters if the requirement table and milestone audit both recognize it. This plan repairs that traceability explicitly and honestly.
Output: `REQUIREMENTS.md` updated for `VER-04` and a regenerated milestone audit that no longer treats Phase 3 as orphaned.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md
@.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
@.planning/phases/07-rules-conformance-verification-backfill/07-02-SUMMARY.md
@/Users/gabrielwillen/.codex/get-shit-done/workflows/audit-milestone.md

<interfaces>
From .planning/REQUIREMENTS.md:

```text
- [ ] **VER-04**: Add conformance coverage for the library rules captured in `rules.md`.
| VER-04 | Phase 7 | Pending |
```

From .planning/v1.0-MILESTONE-AUDIT.md:

```text
| 3 | unverified | missing | Plan summaries exist but no phase verification artifact |
| `VER-04` | 7 | `[ ]` | present | missing | orphaned |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair `VER-04` traceability and regenerate the milestone audit from real evidence</name>
  <files>.planning/REQUIREMENTS.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <action>After confirming `03-VERIFICATION.md` exists and passes, update `REQUIREMENTS.md` so `VER-04` is marked complete and its traceability row reflects that Phase 7 closed the orphan by backfilling Phase 3 verification evidence. Then rerun the milestone audit workflow from `/Users/gabrielwillen/.codex/get-shit-done/workflows/audit-milestone.md` so `.planning/v1.0-MILESTONE-AUDIT.md` is regenerated from actual verification artifacts instead of hand-edited gap text. The regenerated audit must stop calling Phase 3 or `VER-04` orphaned or missing, but it must continue to report the still-open `VER-05` backfill gap honestly. Do not “fix” the audit by deleting old prose without rerunning the workflow, and do not upgrade the overall audit status to passed while Phase 8 debt remains.</action>
  <verify>
    <automated>bash -lc 'AUDIT=.planning/v1.0-MILESTONE-AUDIT.md && rg -n "^- \\[x\\] \\*\\*VER-04\\*\\*" .planning/REQUIREMENTS.md && rg -n "^\\| VER-04 \\| Phase 7 \\| Complete \\|" .planning/REQUIREMENTS.md && rg -n "^status: gaps_found$" "$AUDIT" && rg -n "^\\| 3 \\| verified \\| `03-VERIFICATION\\.md` \\|" "$AUDIT" && rg -n "03-VERIFICATION\\.md" "$AUDIT" && ! rg -n "VER-04.*orphaned|03-VERIFICATION\\.md does not exist" "$AUDIT" && rg -n "VER-05" "$AUDIT" && rg -n "^\\| 4 \\| unverified \\| missing \\|" "$AUDIT"'</automated>
  </verify>
  <done>The top-level requirement table and milestone audit both recognize the new Phase 3 verification artifact, and only the still-unfinished Phase 8 backfill remains open.</done>
</task>

</tasks>

<verification>
Check that `VER-04` is complete in `REQUIREMENTS.md`, then confirm the regenerated milestone audit mentions `03-VERIFICATION.md` without the old orphan/missing text and still preserves the remaining `VER-05` gap signals.
</verification>

<success_criteria>
`VER-04` is closed at the top level with truthful traceability: the requirement table is complete, the milestone audit sees the new Phase 3 verification artifact, and no unrelated gap is papered over.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rules-conformance-verification-backfill/07-03-SUMMARY.md`
</output>
