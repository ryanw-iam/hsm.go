---
phase: 07-rules-conformance-verification-backfill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hsm.go
  - hsm_rules_model_conformance_test.go
  - hsm_rules_runtime_conformance_test.go
  - hsm_test.go
  - hsm_test_helpers_test.go
autonomous: true
requirements:
  - VER-04
gap_closure: true
must_haves:
  truths:
    - "The shipped Phase 3 rules-conformance claims are re-proven against current code instead of being trusted from old summaries alone."
    - "Any failure uncovered during re-verification is remediated only inside the existing Phase 3 rules slice, and only if that tiny fix is required to keep the evidence truthful."
    - "The focused rules-slice command and the canonical workspace gate both pass before any Phase 7 document claims `VER-04` closure."
  artifacts:
    - path: "hsm_rules_model_conformance_test.go"
      provides: "Focused define-time rule-ID coverage for every shipped `define_panic` Phase 3 row"
    - path: "hsm_rules_runtime_conformance_test.go"
      provides: "Focused runtime and exemplar rule-ID coverage for every shipped non-`define_panic` Phase 3 row"
    - path: "hsm.go"
      provides: "Only the already-shipped rules-enforcement seam, reserved for tiny corrective fixes if re-verification disproves an existing claim"
  key_links:
    - from: "hsm_rules_model_conformance_test.go"
      to: "hsm.go"
      via: "Define-time conformance cases must still match the current builder validation paths"
      pattern: "TestModelRulesConformance|Entry\\(|Exit\\(|Activity\\(|Defer\\(|Initial\\(|Choice\\(|ShallowHistory|DeepHistory"
    - from: "hsm_rules_runtime_conformance_test.go"
      to: "hsm.go"
      via: "Runtime/exemplar conformance cases must still match current dispatch, call, timer, and fallback semantics"
      pattern: "TestRuntimeRulesConformance|Dispatch\\(|Set\\(|Call\\(|After\\(|Every\\(|When\\(|AnyEvent"
    - from: "hsm_rules_runtime_conformance_test.go"
      to: "scripts/verify-workspace.sh"
      via: "The same rules slice must survive the canonical workspace gate used for shipping"
      pattern: "go test ./\\.\\.\\.|go test -race -short"
---

<objective>
Re-prove the shipped Phase 3 rules-conformance slice before any backfill document claims `VER-04` closure.

Purpose: Phase 7 closes an audit orphan, not unfinished Phase 3 feature work. This plan verifies the current truth first so later evidence stays honest.
Output: Green focused rules-conformance verification on current code, plus only a tiny Phase 3 correction if fresh evidence disproves an existing claim.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-rules-conformance-verification-backfill/07-RESEARCH.md
@.planning/phases/07-rules-conformance-verification-backfill/07-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-RESEARCH.md
@.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-01-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-02-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-03-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-04-SUMMARY.md
@rules.md
@README.md
@hsm.go
@hsm_rules_model_conformance_test.go
@hsm_rules_runtime_conformance_test.go
@hsm_test.go
@hsm_test_helpers_test.go
@scripts/verify-workspace.sh

<interfaces>
From .planning/phases/07-rules-conformance-verification-backfill/07-VALIDATION.md:

```text
GOCACHE="$PWD/.cache/go-build" go test ./... -run 'TestModelRulesConformance|TestRuntimeRulesConformance' -count=1
GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh
```

From .planning/phases/03-rules-conformance-matrix/03-04-SUMMARY.md:

```text
Phase 3 closes only when the checked-in matrix points to actual rule-ID subtests and the canonical verifier passes.
HSM35 conformance should assert call protocol evidence without depending on race-sensitive effect-versus-operation ordering.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the shipped rules slice and keep any correction microscopic</name>
  <files>hsm.go, hsm_rules_model_conformance_test.go, hsm_rules_runtime_conformance_test.go, hsm_test.go, hsm_test_helpers_test.go</files>
  <action>Run `GOCACHE="$PWD/.cache/go-build" go test ./... -run 'TestModelRulesConformance|TestRuntimeRulesConformance' -count=1`, then run `GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh`. If both commands pass, leave the Phase 3 rules slice unchanged and capture the exact command/output facts for Plan 07-02. If re-verification fails because the current code disproves a shipped Phase 3 claim, fix only the smallest blocking issue inside the existing rules slice listed in this plan. Keep remediation limited to current rules-enforcement or rules-suite seams; do not expand `rules.md`, rewrite the matrix scope, add new feature coverage families, or touch later-phase adversarial work. If a truthful fix would require broader scope than these files, stop and surface that mismatch instead of widening the plan.</action>
  <verify>
    <automated>bash -lc 'GOCACHE="$PWD/.cache/go-build" go test ./... -run '\''TestModelRulesConformance|TestRuntimeRulesConformance'\'' -count=1 && GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh'</automated>
  </verify>
  <done>The current repository truth for Phase 3 is known from fresh command evidence, and any required correction stays microscopic inside the existing rules slice.</done>
</task>

</tasks>

<verification>
Run the focused rules command from `07-VALIDATION.md`, then `GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh`. No later Phase 7 document should claim `VER-04` closure unless both commands are green on the same workspace state.
</verification>

<success_criteria>
Phase 7 has fresh executable proof for the current Phase 3 rules-conformance slice, and any issue uncovered during that proof is either fixed only inside the shipped rules seam or explicitly surfaced as out of scope for this backfill.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rules-conformance-verification-backfill/07-01-SUMMARY.md`
</output>
