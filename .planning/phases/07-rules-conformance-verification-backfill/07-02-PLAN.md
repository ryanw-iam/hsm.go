---
phase: 07-rules-conformance-verification-backfill
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md
  - .planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
autonomous: true
requirements:
  - VER-04
gap_closure: true
must_haves:
  truths:
    - "Phase 3 has a milestone-grade `03-VERIFICATION.md` grounded in fresh command evidence against current code."
    - "`03-VALIDATION.md` no longer reads as draft or pending-only audit debt once the backfill is complete."
    - "The verification report and validation record cite the same `GOCACHE=\"$PWD/.cache/go-build\"` command family, so checker traceability does not rely on inference."
  artifacts:
    - path: ".planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md"
      provides: "Goal-backward verification report for the shipped Phase 3 rules-conformance scope"
    - path: ".planning/phases/03-rules-conformance-matrix/03-VALIDATION.md"
      provides: "Reconciled executed validation record with copy-safe commands and completed statuses"
  key_links:
    - from: ".planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md"
      to: ".planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md"
      via: "The report must tie `rules.md` rows to real matrix evidence and rule-ID test targets"
      pattern: "HSM[0-9][0-9]|TestModelRulesConformance|TestRuntimeRulesConformance"
    - from: ".planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md"
      to: ".planning/phases/03-rules-conformance-matrix/03-02-SUMMARY.md"
      via: "Define-time conformance claims must map back to the shipped model-rule evidence"
      pattern: "define_panic|TestModelRulesConformance"
    - from: ".planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md"
      to: ".planning/phases/03-rules-conformance-matrix/03-03-SUMMARY.md"
      via: "Runtime and exemplar claims must map back to the shipped runtime-rule evidence"
      pattern: "runtime|exemplar|TestRuntimeRulesConformance"
    - from: ".planning/phases/03-rules-conformance-matrix/03-VALIDATION.md"
      to: "scripts/verify-workspace.sh"
      via: "The reconciled validation record must preserve the same canonical gate used by the verification report"
      pattern: "GOCACHE=.*bash scripts/verify-workspace\\.sh"
---

<objective>
Backfill the missing Phase 3 verification artifacts from the fresh rules evidence produced in Plan 07-01.

Purpose: The audit orphan exists because Phase 3 has shipped summaries but no milestone-grade verification artifact, and its validation doc still presents draft intent instead of executed proof.
Output: A truthful `03-VERIFICATION.md` plus a reconciled `03-VALIDATION.md` that both point at the same executed commands.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-rules-conformance-verification-backfill/07-RESEARCH.md
@.planning/phases/07-rules-conformance-verification-backfill/07-VALIDATION.md
@.planning/phases/07-rules-conformance-verification-backfill/07-01-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-RULES-MATRIX.md
@.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md
@.planning/phases/03-rules-conformance-matrix/03-01-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-02-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-03-SUMMARY.md
@.planning/phases/03-rules-conformance-matrix/03-04-SUMMARY.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-VERIFICATION.md
@.planning/phases/02-deterministic-runtime-semantics/02-VERIFICATION.md
@rules.md
@README.md

<interfaces>
From .planning/phases/02-deterministic-runtime-semantics/02-VERIFICATION.md:

```text
Observable Truths
Required Artifacts
Key Link Verification
Requirements Coverage
```

From .planning/phases/07-rules-conformance-verification-backfill/07-VALIDATION.md:

```text
GOCACHE="$PWD/.cache/go-build" go test ./... -run 'TestModelRulesConformance|TestRuntimeRulesConformance' -count=1
GOCACHE="$PWD/.cache/go-build" bash scripts/verify-workspace.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write a milestone-grade `03-VERIFICATION.md` from fresh rules evidence</name>
  <files>.planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md</files>
  <action>Create `.planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md` in the same verification-report shape used by Phases 1, 2, and 5, but scoped strictly to the shipped Phase 3 rules-conformance surface. Derive truths from the Phase 3 roadmap success criteria, `rules.md`, `03-RULES-MATRIX.md`, the four Phase 3 summaries, and the fresh commands from Plan 07-01. Explicitly call out any tiny remediation that Plan 07-01 had to make. Do not write placeholder language, future tense, or copied audit prose; the report must read like a completed verification artifact, not a plan.</action>
  <verify>
    <automated>bash -lc 'REPORT=.planning/phases/03-rules-conformance-matrix/03-VERIFICATION.md && test -f "$REPORT" && rg -n "^# Phase 3: Rules Conformance Matrix Verification Report$" "$REPORT" && rg -n "^\\*\\*Status:\\*\\* passed$" "$REPORT" && rg -n "^### Observable Truths$" "$REPORT" && rg -n "^### Required Artifacts$" "$REPORT" && rg -n "^### Key Link Verification$" "$REPORT" && rg -n "^## Requirements Coverage$" "$REPORT" && rg -n "VER-04" "$REPORT" && rg -n "03-RULES-MATRIX\\.md|rules\\.md|TestModelRulesConformance|TestRuntimeRulesConformance" "$REPORT" && rg -n "scripts/verify-workspace\\.sh|canonical workspace gate" "$REPORT"'</automated>
  </verify>
  <done>`03-VERIFICATION.md` exists, reads like a completed verification report, and ties `VER-04` to concrete matrix, suite, and gate evidence from the current codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile `03-VALIDATION.md` from draft intent to executed evidence</name>
  <files>.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md</files>
  <action>Update `03-VALIDATION.md` so it reflects executed backfill reality instead of draft intent: preserve the original five Phase 3 task rows, replace non-copy-safe Go command text with `GOCACHE="$PWD/.cache/go-build"` command text everywhere it references rule verification, change task rows from `⬜ pending` to completed status values consistent with the executed evidence, and move the file out of draft/pending sign-off language. Keep the validation map aligned to the shipped Phase 3 execution shape plus the Phase 7 backfill commands; do not invent Phase 7-only rows, manual verification requirements, or new rule categories.</action>
  <verify>
    <automated>bash -lc 'VALIDATION=.planning/phases/03-rules-conformance-matrix/03-VALIDATION.md && ! rg -n "^status: draft|⬜ pending|Approval: pending" "$VALIDATION" && rg -n "03-01-01|03-01-02|03-02-01|03-03-01|03-04-01" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" go test ./... -run '\''TestModelRulesConformance\\|TestRuntimeRulesConformance'\'' -count=1" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" go test ./... -run '\''TestModelRulesConformance'\'' -count=1" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" go test ./... -run '\''TestRuntimeRulesConformance'\'' -count=1" "$VALIDATION" && rg -n "GOCACHE=\"\\$PWD/.cache/go-build\" bash scripts/verify-workspace\\.sh" "$VALIDATION"'</automated>
  </verify>
  <done>`03-VALIDATION.md` no longer communicates draft/pending debt and records the same executable commands used to backfill Phase 3 evidence.</done>
</task>

</tasks>

<verification>
Confirm `03-VERIFICATION.md` exists with the standard verification-report sections, then confirm `03-VALIDATION.md` no longer contains draft/pending markers and uses the same copy-safe command family that actually proved Phase 3.
</verification>

<success_criteria>
Phase 3 now has both a milestone-grade verification report and a reconciled validation record, and the two artifacts agree on what was executed to satisfy `VER-04`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rules-conformance-verification-backfill/07-02-SUMMARY.md`
</output>
