---
phase: 01-coverage-baseline-and-release-gates
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .github/workflows/unit-tests.yml
  - .github/workflows/release.yml
  - .github/workflows/auto-release.yml
  - scripts/verify-workspace.sh
  - .planning/phases/01-coverage-baseline-and-release-gates/01-PATH-MATRIX.md
autonomous: true
requirements:
  - VER-02
must_haves:
  truths:
    - "Maintainers and CI use one canonical Phase 1 verification command across `hsm`, `kind`, and `muid`."
    - "Release automation fails before shipping when the baseline verification suite fails."
    - "Phase 1 evidence names covered, failing, and deferred path groups explicitly instead of hiding them behind one aggregate coverage number."
  artifacts:
    - path: scripts/verify-workspace.sh
      provides: Canonical Phase 1 verification entry point
    - path: .planning/phases/01-coverage-baseline-and-release-gates/01-PATH-MATRIX.md
      provides: Workspace-wide path matrix with covered, failing, and deferred status
    - path: .github/workflows/unit-tests.yml
      provides: CI job wired to the canonical verification command
    - path: .github/workflows/release.yml
      provides: Tag-release job blocked on the same canonical verification command
    - path: .github/workflows/auto-release.yml
      provides: Automated release path blocked on the same canonical verification command
  key_links:
    - from: .github/workflows/unit-tests.yml
      to: scripts/verify-workspace.sh
      via: workflow run step invoking the canonical gate
      pattern: "verify-workspace\\.sh"
    - from: .github/workflows/release.yml
      to: scripts/verify-workspace.sh
      via: release workflow verification step before goreleaser
      pattern: "verify-workspace\\.sh"
    - from: .github/workflows/auto-release.yml
      to: scripts/verify-workspace.sh
      via: auto-release verification step before tag or release creation
      pattern: "verify-workspace\\.sh"
    - from: .planning/phases/01-coverage-baseline-and-release-gates/01-PATH-MATRIX.md
      to: hsm_test_helpers_test.go
      via: path matrix entries referencing the concrete root and helper coverage artifacts produced in Wave 1
      pattern: "Covered|Failing|Deferred"
---

<objective>
Create the canonical Phase 1 release gate and path-matrix evidence so a clean checkout can prove whether the workspace is safe to ship.

Purpose: Baseline coverage is only meaningful if the same command blocks release automation and leaves an audit-friendly record of what is covered, failing, or deferred.
Output: One verification script, release-wired workflows, and a workspace-wide path matrix.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-CONTEXT.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-RESEARCH.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-VALIDATION.md
@.github/workflows/unit-tests.yml
@.github/workflows/release.yml
@.github/workflows/auto-release.yml

<interfaces>
Canonical Phase 1 commands from research:

```bash
go test ./... ./kind/... ./muid/...
go test -race -short ./... ./kind/... ./muid/...
```

Locked decisions from 01-CONTEXT.md:

- one canonical verification command for maintainers and CI
- evidence must be a path matrix, not a headline coverage number
- deferred gaps remain visible but do not fail Phase 1 solely because they belong to later phases
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical workspace verification command</name>
  <files>scripts/verify-workspace.sh</files>
  <action>Create an executable shell script that becomes the single Phase 1 verification entry point. It should run the quick workspace-wide baseline command and the blocking race-enabled command in a clear, sectioned order; stop on failure; and print package-scoped status so maintainers can see which package failed. Keep the command aligned with the Phase 1 scope only: `hsm`, `kind`, and `muid`, without pulling in deferred Phase 2-4 verification modes.</action>
  <verify>
    <automated>bash scripts/verify-workspace.sh</automated>
  </verify>
  <done>The repository has one executable verification command that maintainers and CI can both call for the Phase 1 gate.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the Phase 1 path matrix evidence</name>
  <files>.planning/phases/01-coverage-baseline-and-release-gates/01-PATH-MATRIX.md</files>
  <action>Create a maintainable path matrix organized by package and behavior family. Each row should name the concrete test file or suite covering the path, mark it as `covered`, `failing`, or `deferred`, and point deferred work to the roadmap phase that owns it. Do not replace this with a headline coverage percentage. Include the known deferred families from Phase 2, Phase 3, and Phase 4 so reviewers can see what is intentionally outside the Phase 1 gate.</action>
  <verify>
    <automated>rg -n "covered|failing|deferred|Phase 2|Phase 3|Phase 4" .planning/phases/01-coverage-baseline-and-release-gates/01-PATH-MATRIX.md</automated>
  </verify>
  <done>Reviewers can inspect one evidence file and immediately see which `hsm`, `kind`, and `muid` paths are covered now, still failing, or explicitly deferred.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the canonical gate into CI and release workflows</name>
  <files>.github/workflows/unit-tests.yml, .github/workflows/release.yml, .github/workflows/auto-release.yml</files>
  <action>Update GitHub Actions so all relevant verification and release paths invoke `scripts/verify-workspace.sh` before continuing to publish or tag. Align Go setup with the workspace toolchain source of truth instead of a hard-coded older version where practical, but do not add unrelated CI churn. Ensure the release and auto-release workflows cannot ship artifacts if the canonical verification script fails.</action>
  <verify>
    <automated>rg -n "verify-workspace\\.sh|go-version-file|go-version" .github/workflows/unit-tests.yml .github/workflows/release.yml .github/workflows/auto-release.yml</automated>
  </verify>
  <done>CI and release automation share the same Phase 1 gate, and failed baseline verification blocks shipping instead of merely reporting advisory failures elsewhere.</done>
</task>

</tasks>

<verification>
Run `bash scripts/verify-workspace.sh` after wiring the workflows. The script must go non-zero on any baseline failure, and the workflow files must visibly call that same script before test or release steps proceed.
</verification>

<success_criteria>
There is one canonical Phase 1 verification command, release automation cannot bypass it, and maintainers have a path-matrix artifact that makes coverage status and deferred ownership obvious.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coverage-baseline-and-release-gates/01-03-SUMMARY.md`
</output>
