---
phase: 01-coverage-baseline-and-release-gates
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - kind/kind_test.go
  - muid/muid.go
  - muid/muid_test.go
  - muid/muid_paths_test.go
  - muid/muid_bench_test.go
autonomous: true
requirements:
  - VER-01
  - VER-02
must_haves:
  truths:
    - "The helper modules `kind` and `muid` both contribute real baseline evidence instead of being weak side suites."
    - "The public `muid` API no longer reports a false green at the workspace level."
    - "Helper-module edge cases and contract behavior are verified by dedicated tests, not buried in benchmark files or omitted entirely."
  artifacts:
    - path: kind/kind_test.go
      provides: Explicit coverage for ancestry encoding, deduplication, and kind matching
    - path: muid/muid_paths_test.go
      provides: Dedicated `muid` behavior-path coverage separated from benchmarks
    - path: muid/muid.go
      provides: Production fix or guarantee-preserving implementation update for exposed baseline failure
    - path: muid/muid_bench_test.go
      provides: Benchmark-only content after behavior assertions are normalized
  key_links:
    - from: kind/kind_test.go
      to: kind/kind.go
      via: direct assertions on List, Make, and Is across base-chain scenarios
      pattern: "List|Make|Is"
    - from: muid/muid_paths_test.go
      to: muid/muid.go
      via: direct assertions on NewGenerator, Generator.ID, Make, MakeString, and MUID.String
      pattern: "NewGenerator|ID|Make|MakeString|String"
    - from: muid/muid.go
      to: muid/muid_paths_test.go
      via: implementation and tests agree on the package's monotonic uniqueness contract
      pattern: "monotonic|sort|clock|counter|shard"
---

<objective>
Bring `kind` and `muid` up to the same baseline bar as the root runtime, including closure of the currently exposed `muid` failure in the explicit workspace gate.

Purpose: Phase 1 is blocked until helper modules are real release-gated dependencies, not advisory coverage afterthoughts.
Output: Dedicated helper-module coverage plus a green helper-module baseline command.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-CONTEXT.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-RESEARCH.md
@.planning/phases/01-coverage-baseline-and-release-gates/01-VALIDATION.md
@kind/kind.go
@kind/kind_test.go
@muid/muid.go
@muid/muid_test.go
@muid/muid_bench_test.go

<interfaces>
From kind/kind.go:

```go
type Kind = uint64
func List(t Kind) [depthMax]Kind
func Make(bases ...Kind) Kind
func Is(kind Kind, bases ...Kind) bool
```

From muid/muid.go:

```go
type Config struct {
	MachineID       uint64
	TimestampBitLen int
	MachineIDBitLen int
	Epoch           int64
}

type MUID uint64
func (m MUID) String() string

type Generator struct { /* opaque */ }
func NewGenerator(config Config, shardIndex uint64, shardBitLen int) *Generator
func (g *Generator) ID() MUID
func Make() MUID
func MakeString() string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build explicit `kind` path coverage</name>
  <files>kind/kind_test.go</files>
  <action>Replace the empty `TestKinds` placeholder with focused coverage for `List`, `Make`, and `Is`. Assert unique ID allocation, inheritance flattening, duplicate-base deduplication, ancestry slot order, self-matching, transitive-base matching, and rejection of unrelated bases. Include an edge-case assertion around the 8-bit type ID space so overflow or wrap behavior is at least documented by a test instead of being implicit.</action>
  <verify>
    <automated>go test ./kind/...</automated>
  </verify>
  <done>`kind` has dedicated assertions for its full exported surface and no longer contributes a nominal-but-empty test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize `muid` behavior tests and close the exposed failure</name>
  <files>muid/muid.go, muid/muid_test.go, muid/muid_paths_test.go, muid/muid_bench_test.go</files>
  <behavior>
    - Test 1: `NewGenerator` applies defaults, masks oversized machine IDs, and produces monotonic IDs within a single generator.
    - Test 2: `Generator.ID` clamps clock regression to the last emitted timestamp and advances virtually on counter overflow.
    - Test 3: `Make` and `MakeString` remain unique across default shards and satisfy the package's documented monotonic uniqueness contract.
    - Test 4: Benchmark files keep performance comparisons only; contract assertions live in normal test files.
  </behavior>
  <action>Move behavioral assertions out of `muid_bench_test.go` into `muid_paths_test.go`, then expand coverage for `Config`, `NewGenerator`, `Generator.ID`, `Make`, `MakeString`, and `MUID.String`. Fix `muid.go` so the default public path matches the package's documented monotonic uniqueness claim and stops failing `TestSortability/MUID` in the explicit workspace gate. If a helper assertion in `muid_test.go` only proves a weak proxy today, replace it with direct contract assertions instead of preserving low-signal checks.</action>
  <verify>
    <automated>go test ./muid/...</automated>
  </verify>
  <done>`muid` behavior coverage lives in normal test files, the helper-module suite passes cleanly, and the exposed sortability/monotonicity failure is either fixed in code or replaced by a stricter contract-accurate implementation that still honors package docs.</done>
</task>

</tasks>

<verification>
Run `go test ./kind/...` after Task 1 and `go test ./muid/...` after Task 2. After both tasks land, run `go test ./kind/... ./muid/...` to confirm the helper modules are green together before Wave 2.
</verification>

<success_criteria>
`kind` and `muid` both supply explicit baseline coverage, the helper-module workspace command goes green, and the repo no longer hides a failing `muid` contract behind root-only automation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coverage-baseline-and-release-gates/01-02-SUMMARY.md`
</output>
